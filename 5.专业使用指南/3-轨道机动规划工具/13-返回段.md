# 返回段

返回段用来将控制变量返回到他的父级段，从而控制整个任务控制序列的运行。如果任务控制序列段的运行状态为迭代运行，则在该段后，任务控制序列段不会运行任何段。返回段有两种状态设置选项，分别是<img src="media/5.3轨道机动规划工具/image-26.png" alt="" no-view />和<img src="media/5.3轨道机动规划工具/image-27.png" alt="" no-view />，可以通过点击圆形按钮进行选择。当选择开启的状态时，该段是运行的。当选择关闭的状态时，运行整个任务控制序列段，该段是不发挥作用的。
