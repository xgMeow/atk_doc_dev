# 通信约束

## 功能说明

通信约束用于分析发射器与接收器之间的通信链路是否满足指定条件，适用于卫星通信、星间链路、地面站通信、遥测遥控及数据传输等任务。系统按频率、接收功率、信噪比、误码率等链路指标判断当前通信链路是否可用：链路指标数值满足设定范围则该访问满足通信约束，否则判定为无效访问。

所有参数均支持设置**最小值**/**最大值**，并可勾选**反向选择**（排除设定范围内的访问）。

## 参数说明

<Entry meta="量纲：频率|单位：GHz" name="频率" desc="通信链路的工作频率范围。" objects="发射器,接收器">

限制发射器或接收器响应的频率范围。可通过【波段】下拉列表选择特定波段，或选择【自定义】自行指定频率范围。

勾选【最小值】或【最大值】并输入数值后，即可限定允许访问落入的取值范围；勾选【反向选择】后，落入该范围的访问将被剔除，仅保留范围之外的访问。

计算公式见 [频率](../../../04-理论基础/02-约束配置/04-通信约束.md#frequency)。

</Entry>

<Entry meta="量纲：温度|单位：K" name="系统噪声温度" desc="通信系统的噪声温度。" objects="接收器">

勾选【最小值】或【最大值】并输入数值后，即可限定允许访问落入的取值范围；勾选【反向选择】后，落入该范围的访问将被剔除，仅保留范围之外的访问。

计算公式见 [系统噪声温度](../../../04-理论基础/02-约束配置/04-通信约束.md#system-noise-temperature)。

</Entry>

<Entry meta="量纲：功率|单位：dBW" name="接收到的各向同性功率" desc="假设接收端为理想各向同性天线时能够接收到的功率水平。" objects="发射器,接收器">

勾选【最小值】或【最大值】并输入数值后，即可限定允许访问落入的取值范围；勾选【反向选择】后，落入该范围的访问将被剔除，仅保留范围之外的访问。

计算公式见 [接收到的各向同性功率](../../../04-理论基础/02-约束配置/04-通信约束.md#rcvd-isotropic-power)。

</Entry>

<Entry meta="量纲：功率密度|单位：dBW/m²" name="通量密度" desc="接收端单位面积上的电磁波功率密度。" objects="发射器,接收器">

勾选【最小值】或【最大值】并输入数值后，即可限定允许访问落入的取值范围；勾选【反向选择】后，落入该范围的访问将被剔除，仅保留范围之外的访问。

计算公式见 [通量密度](../../../04-理论基础/02-约束配置/04-通信约束.md#flux-density)。

</Entry>

<Entry meta="量纲：功率|单位：dBW" name="链路等效各向同性辐射功率" desc="在发射机与接收机之间的链路方向上所计算的等效各向同性辐射功率。" objects="发射器,接收器">

勾选【最小值】或【最大值】并输入数值后，即可限定允许访问落入的取值范围；勾选【反向选择】后，落入该范围的访问将被剔除，仅保留范围之外的访问。

计算公式见 [链路等效各向同性辐射功率](../../../04-理论基础/02-约束配置/04-通信约束.md#link-eirp)。

</Entry>

<Entry meta="量纲：频率|单位：GHz" name="多普勒频移" desc="因相关物体相对速度引起的频率偏移量。" objects="发射器,接收器">

勾选【最小值】或【最大值】并输入数值后，即可限定允许访问落入的取值范围；勾选【反向选择】后，落入该范围的访问将被剔除，仅保留范围之外的访问。

计算公式见 [多普勒频移](../../../04-理论基础/02-约束配置/04-通信约束.md#doppler-shift)。

</Entry>

<Entry meta="量纲：信噪比谱密度|单位：dB·Hz" name="C/No" desc="在接收天线输出处测得的载波功率与噪声功率谱密度之比。" objects="发射器,接收器">

勾选【最小值】或【最大值】并输入数值后，即可限定允许访问落入的取值范围；勾选【反向选择】后，落入该范围的访问将被剔除，仅保留范围之外的访问。

计算公式见 [C/N₀](../../../04-理论基础/02-约束配置/04-通信约束.md#c-no)。

</Entry>

<Entry meta="量纲：功率|单位：dBW" name="接收机输入功率" desc="接收机前端放大器输入处的接收信号功率。" objects="发射器,接收器">

勾选【最小值】或【最大值】并输入数值后，即可限定允许访问落入的取值范围；勾选【反向选择】后，落入该范围的访问将被剔除，仅保留范围之外的访问。

计算公式见 [接收机输入功率](../../../04-理论基础/02-约束配置/04-通信约束.md#receiver-input-power)。

</Entry>

<Entry meta="量纲：信噪比|单位：dB" name="C/N" desc="真实载波功率与噪声功率之比。" objects="发射器,接收器">

勾选【最小值】或【最大值】并输入数值后，即可限定允许访问落入的取值范围；勾选【反向选择】后，落入该范围的访问将被剔除，仅保留范围之外的访问。

计算公式见 [C/N](../../../04-理论基础/02-约束配置/04-通信约束.md#c-n)。

</Entry>

<Entry name="误码率" desc="比特错误率。" objects="发射器,接收器">

勾选【最小值】或【最大值】并输入数值后，即可限定允许访问落入的取值范围；勾选【反向选择】后，落入该范围的访问将被剔除，仅保留范围之外的访问。

计算公式见 [误码率](../../../04-理论基础/02-约束配置/04-通信约束.md#bit-error-rate)。

</Entry>

<Entry name="链路裕量" desc="链路实际性能相对于最低通信需求所留有的余量。" objects="发射器,接收器">

勾选【最小值】或【最大值】并输入数值后，即可限定允许访问落入的取值范围；勾选【反向选择】后，落入该范围的访问将被剔除，仅保留范围之外的访问。

计算公式见 [链路裕量](../../../04-理论基础/02-约束配置/04-通信约束.md#link-margin)。

</Entry>

<Entry meta="量纲：信噪比|单位：dB" name="Eb/No" desc="每比特能量与噪声功率谱密度之比。" objects="发射器,接收器">

勾选【最小值】或【最大值】并输入数值后，即可限定允许访问落入的取值范围；勾选【反向选择】后，落入该范围的访问将被剔除，仅保留范围之外的访问。

计算公式见 [Eb/N₀](../../../04-理论基础/02-约束配置/04-通信约束.md#eb-no)。

</Entry>

<Entry name="极化相对角" desc="发射天线极化面与接收天线极化面之间的相对夹角。" objects="发射器,接收器">

勾选【最小值】或【最大值】并输入数值后，即可限定允许访问落入的取值范围；勾选【反向选择】后，落入该范围的访问将被剔除，仅保留范围之外的访问。

计算公式见 [极化相对角](../../../04-理论基础/02-约束配置/04-通信约束.md#polarization-relative-angle)。

</Entry>

<Entry meta="量纲：灵敏度|单位：dB/K" name="天线性能品质因子" desc="在链路方向上的链路预算接收增益值与接收器总噪声温度之比。" objects="发射器,接收器">

勾选【最小值】或【最大值】并输入数值后，即可限定允许访问落入的取值范围；勾选【反向选择】后，落入该范围的访问将被剔除，仅保留范围之外的访问。

计算公式见 [天线性能品质因子](../../../04-理论基础/02-约束配置/04-通信约束.md#g-t)。

</Entry>
