---
title: 约束配置
index: false
---

对象在参与可见性、覆盖性分析时，可在【属性】→【约束】中启用各类约束，以限定满足任务要求的访问时段。

约束在属性中按 **Tab 页** 组织，如 基本、太阳、时间、通信 等。本册各页描述对应约束界面的通用定义与参数含义。不同对象开放的约束页可能不同（例如通信约束仅与发射器、接收器相关），各对象的开放情况见 对象属性「约束」段的链接表。

<Catalog />
