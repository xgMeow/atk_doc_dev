# SelectObject

## 作用

打开相关界面，在界面中选择ATK算法模型/对象并返回

## 语法

```atks
SelectObject(typeName)
```

## 补充说明

输入类型名称 `typeName`，弹出对象选择界面，返回所选择的ATK算法模型/对象

## 示例

::: details open **选择ATK对象**

```atks
sat = SelectObject("Satellite")
fac = SelectObject("Facility")
```

:::
