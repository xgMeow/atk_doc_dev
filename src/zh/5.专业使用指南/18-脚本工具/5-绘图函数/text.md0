# text

## 作用

向数据点添加文本说明

## 语法

```
    text(x,y,txt)
```

## 补充说明

- `text(x,y,txt)` ：使用由 txt 指定的文本，向当前坐标区中的一个或多个数据点添加文本说明。

## 示例

::: details open **向数据点添加文本说明**

绘制一条正弦曲线。在点 (π,0) 处，添加文本说明 sin(π)。

```
    x = linspace(0,2*pi,40);
    y = sin(x);
    str = "wangshuo"
    plot(x,y)
    text( pi, 0, str)
```

:::
