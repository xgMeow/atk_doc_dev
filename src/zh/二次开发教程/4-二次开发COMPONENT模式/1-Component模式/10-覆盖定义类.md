# 覆盖定义类


## 类 ICoverageDefinition, 继承自类IAtkObject

 

属性类型：覆盖定义

举  例 ：
```cpp
ICoverageDefinition* plCoverageDefinition = (ICoverageDefinition*)pIScen->GetChildren()->New(eCoverageDefinition,"ICoverageDefinition")
```
 

### GetGrid()

函数说明：获得网格

返回参数类型： `ICvGrid*`

返回参数说明：网格

举  例： 
```cpp
ICvGrid* plCvGrid = plCoverageDefinition->GetGrid(); 
```
 
 ### GetGraphics()

函数说明：获得二维属性

返回参数类型：`ICvGraphics*`

返回参数说明：二维属性指针

举    例：
```cpp
ICvGraphics* pICvGraphics = pICoverageDefinition->GetGraphics();
```
 

## 类 ICvGrid
 
###  GetBoundsType()

函数说明：获得覆盖区域类型

返回参数类型： `ECvBounds`

返回参数说明： 覆盖区域类型

举  例： 
```cpp
ECvBounds* pECvBounds = pICvGrid->GetBoundsType();
```
 
###  SetBoundsType()

函数说明： 设置覆盖区域类型(扩展函数)

输入类型： `ECvBounds`

输入说明： 覆盖区域类型

举  例： 
```cpp
pICvGrid->SetBoundsType(eBoundsGlobal);
```
  
###  GetBounds() 
 
函数说明：获得覆盖区域

返回参数类型： `ICvBounds*`

返回参数说明：覆盖区域

举  例： 
```cpp
ICvBounds* pICvBounds = pICvGrid->GetBounds(); 
```
  
###  GetResolutionType() 
 
函数说明：获得分辨率类型

返回参数类型： `ECvResolution`

返回参数说明： 分辨率类型

举  例： 
```cpp
ECvResolution* pECvResolution = pICvGrid->GetResolutionType(); 
``` 

###  SetResolutionType()
 
函数说明： 设置分辨率类型(扩展函数)

输入类型： `EcvResolution`

输入说明： 分辨率类型

举  例： 
```cpp
pICvGrid->SetResolutionType(eResolutionArea);
``` 

### GetResolution() 
 
函数说明：获得分辨率

返回参数类型： `ICvResolution*`

返回参数说明：分辨率

举  例 ：
```cpp
ICvResolution* pICvResolution = plCvGrid->GetResolution(); 
```

## 类 ICvBounds

 
## 类 ICvBoundsGlobal
 
> [!info]
> 类 ICvBoundsGlobal,   继承自类ICvBounds


属性类型： `ICvBoundsGlobal*`

属性说明：覆盖区域子类：全球

举  例 ：
```cpp
plCvGrid->SetBoundsType(eBoundsGlobal);

ICvBoundsGlobal* pICvBoundsGlobal = (ICvBoundsGlobal*)pICvGrid->GetBounds();
```
 
## 类 ICvBoundsLatLonRegion

> [!info]
> 类 ICvBoundsLatLonRegion, 继承自类ICvBounds

属性类型： `ICvBoundsLatLonRegion*`

属性说明：覆盖区域子类：经纬度区域

举  例 ：
```cpp
plCvGrid->SetBoundsType(eBoundsLatLonRegion);

ICvBoundsLatLonRegion* pICvBoundsLatLonRegion = (ICvBoundsLatLonRegion*)plCvGrid>GetBounds();
```
  
### GetMinLatitude() 
 
函数说明：获得最小纬度

返回参数类型： `double`

返回参数说明：最小纬度

举  例 ：
```cpp
double dMinLat = pICvBoundsLatLonRegion->GetMinLatitude();
```

### SetMinLatitude()

函数说明：设置最小纬度(扩展函数)

输入类型： `double`
 
输入说明： 最小纬度

举  例： 
```cpp
pICvBoundsLatLonRegion->SetMinLatitude (-90);
```
  
###  GetMaxLatitude() 
  
函数说明：获得最大纬度

返回参数类型： `double`

返回参数说明： 最大纬度

举  例： 
```cpp
double dMaxLat = pICvBoundsLatLonRegion->GetMaxLatitude();
``` 

###  SetMaxLatitude()
  
函数说明： 设置最大纬度(扩展函数)

输入类型： `double`

输入说明： 最大纬度

举  例： 
```cpp
pICvBoundsLatLonRegion->SetMaxLatitude (90);
```
  
###  GetMinLongitude() 
  
函数说明：获得最小经度

返回参数类型： `double`

返回参数说明： 最小经度

举  例： 
```cpp
double dMinLon = pICvBoundsLatLonRegion->GetMinLongitude();
```

###  SetMinLongitude()

函数说明： 设置最小经度(扩展函数)

输入类型： `double`

输入说明： 最小经度

举  例： 
```cpp
pICvBoundsLatLonRegion->SetMinLongitude(-180);
```
 
###  GetMaxLongitude()

函数说明：获得最大经度

返回参数类型： `double`

返回参数说明： 最大经度


举  例： 
```cpp
double dMaxLon = pICvBoundsLatLonRegion->GetMaxLongitude();
```
 
###  SetMaxLongitude()

函数说明： 设置最大经度(扩展函数)

输入类型： `double`

输入说明： 最大经度

举  例： 
```cpp
pICvBoundsLatLonRegion->SetMaxLongitude (180);
```
  
## 类 ICvBoundsLat 
 
> [!info]
> 类 ICvBoundsLat ，继承自类 ICvBounds
 
属性类型： `ICvBoundsLat*`

属性说明： 覆盖区域子类： 纬度区域

举  例： 
```cpp
pICvGrid->SetBoundsType(eBoundsLat);

ICvBoundsLat* pICvBoundsLat = (ICvBoundsLat*)pICvGrid->GetBounds();
```
 
###  GetMinLatitude()

函数说明：获得最小纬度

返回参数类型： `double`

返回参数说明： 最小纬度

举  例： 
```cpp
double dMinLat = ICvBoundsLat->GetMinLatitude();
```
 
###  SetMinLatitude()

函数说明： 设置 最小纬度(扩展函数)

输入类型： `double`

输入说明： 最小纬度

举  例： 
```cpp
ICvBoundsLat->SetMinLatitude(-90);
```

###  GetMaxLatitude()

函数说明：获得最大纬度

返回参数类型： `double`

返回参数说明： 最大纬度

举  例： 
```cpp
double dMaxLat = ICvBoundsLat->GetMaxLatitude();
```
 
###  SetMaxLatitude()
 
函数说明： 设置最大纬度(扩展函数)

输入类型： `double`

输入说明： 最大纬度

举  例： 
```cpp
ICvBoundsLat->SetMaxLatitude(90);
```
  
## 类 ICvBoundsLatLine 

> [!info]
> 类 ICvBoundsLatLine ，继承自类 ICvBounds

属性类型： `ICvBoundsLatLine*`

属性说明： 覆盖区域子类： 纬度线

举  例： 
```cpp
pICvGrid->SetBoundsType (eBoundsLatLine);

ICvBoundsLatLine* pICvBoundsLatLine = (ICvBoundsLatLine*) pICvGrid->GetBounds();
```
  
###  GetLatitude()

函数说明：获得纬度

返回参数类型： `double`

返回参数说明： 纬度

举  例： 
```cpp
double dLat = ICvBoundsLatLine->GetLatitude(); 
```

###  SetLatitude()
  
函数说明： 设置纬度(扩展函数)

输入类型： `double`

输入说明： 纬度

举  例： 
```cpp
ICvBoundsLatLine->SetLatitude (90);
```
  
###  GetMinLongitude() 
 
函数说明：获得最小经度

返回参数类型： `double`

返回参数说明： 最小经度

举  例： 
```cpp
double dMinLon = ICvBoundsLatLine->GetMinLongitude(); 
```
 
###  SetMinLongitude()
 
函数说明： 设置最小经度(扩展函数)

输入类型： `double`

输入说明： 最小经度

举  例： 
```cpp
ICvBoundsLatLine->SetMinLongitude(-180);
```
  
###  GetMaxLongitude() 
 
函数说明：获得最大经度

返回参数类型： `double`

返回参数说明： 最大经度

举  例： 

```cpp
double dMaxLon = ICvBoundsLatLine->GetMaxLongitude(); 
```
  
###  SetMaxLongitude()
 
函数说明： 设置最大经度(扩展函数)

输入类型： `double`

输入说明： 最大经度

举  例： 
```cpp
ICvBoundsLatLine->SetMaxLongitude(180);
```
  
## 类 ICvBoundsLonLine

> [!info]
> 类ICvBoundsLonLine ，继承自类 ICvBounds

属性类型： `ICvBoundsLonLine*`

属性说明： 覆盖区域子类： 经度线

举  例： 
```cpp
pICvGrid->SetBoundsType (eBoundsLonLine);

ICvBoundsLonLine* pICvBoundsLonLine = (ICvBoundsLonLine*)pICvGrid->GetBounds();
```
  
###  GetLongitude() 
 
函数说明：获得经度

返回参数类型： `double`

返回参数说明： 经度

举  例： 
```cpp
double dLon = pICvBoundsLonLine->GetLongitude();
```

###  SetLongitude()
 
函数说明： 设置经度(扩展函数)

输入类型： double

输入说明： 经度

举  例： 
```cpp
pICvBoundsLonLine->SetLongitude (90);
```
  
###  GetMinLatitude() 
  
函数说明：获得最小纬度

返回参数类型： `double`

返回参数说明： 最小纬度

举  例： 
```cpp
double dMinLat = pICvBoundsLonLine->GetMinLatitude(); 
```
  
###  SetMinLatitude()
 
函数说明： 设置 最小纬度(扩展函数)

输入类型： `double`

输入说明： 最小纬度

举  例： 
```cpp
pICvBoundsLonLine->SetMinLatitude(-90);
```
  
###  GetMaxLatitude() 
 
函数说明：获得最大纬度

返回参数类型： `double`

返回参数说明： 最大纬度

举  例： 
```cpp
double dMaxLat = pICvBoundsLonLine->GetMaxLatitude(); 
``` 

###  SetMaxLatitude()
 
函数说明： 设置最大纬度(扩展函数)

输入类型： `double`

输入说明： 最大纬度

举  例： 
```cpp
pICvBoundsLonLine->SetMaxLatitude(90);
```
 
## 类ICvResolution
 
## 类 ICvResolutionArea

> [!info]
> 类 ICvResolutionArea, 继承自类 ICvResolution

属性类型： `ICvResolutionArea*`

属性说明：分辨率子类：区域面积

举  例： 
```cpp
plCvGrid->SetResolutionType (eResolutionArea);

ICvResolutionArea*plCvResolutionArea  =(ICvResolutionArea*)pICvGrid->GetResolution();
```
 
### GetArea() 
 
函数说明：获得面积

返回参数类型： `double`

返回参数说明：面积

举  例 ：
```cpp
double dArea = pICvResolutionArea>GetArea();
```
  
### SetArea()
 
函数说明：设置面积网格分辨率(扩展函数)

输入类型： `double`

输入说明：面积

举  例： 
```cpp
pICvResolutionArea>SetArea(309800);
```
  
## 类 ICvResolutionDistance

> [!info]
> 类 ICvResolutionDistance, 继承自类 ICvResolution

属性类型： `ICvResolutionDistance*`

属性说明：分辨率子类：距离

举  例： 
```cpp
pICvGrid->SetResolutionType (eResolutionDistance);

ICvResolutionDistance*pICvResolutionDistance =(ICvResolutionDistance *) pICvGrid->GetResolution();
```
  
### GetDistance() 
 
函数说明：获得距离

返回参数类型： `double`

返回参数说明：距离

举  例 ：
```cpp
double dDistance = plCvResolutionArea->GetDistance(); 
```
  
### SetDistance()
 
函数说明：设置距离网格分辨率(扩展函数)

输入类型： `double`

输入说明：距离网格分辨率

举  例 ：
```cpp
pICvResolutionArea->SetDistance(556);
```
  
## 类 ICvResolutionLatLon

  
> [!info]
> 类 ICvResolutionLatLon, 继承自类 ICvResolution


属性类型： `ICvResolutionLatLon*`

属性说明：分辨率子类：经纬度

举  例： 
```cpp
plCvGrid->SetResolutionType (eResolutionLatLon);

ICvResolutionLatLon*pICvResolutionLatLon=(ICvResolutionLatLon*) pICvGrid->GetResolution();
```
  
### GetLatLon() 
 
函数说明：获得经纬度

返回参数类型： `double`

返回参数说明：经纬度

举  例 ：
```cpp
double dLatLon = plCvResolutionArea>GetLatLon(); 
```
  
### SetLatLon()
 
函数说明：设置经纬度网格分辨率(扩展函数)

输入类型： `double`

输入说明：经纬度网格分辨率

举  例 ：
```cpp
pICvResolutionArea->SetLatLon(5);
```
 
## 覆盖定义二维

### 类ICvGraphics

举    例：
```cpp
ICvGraphics* pICvGraphics = pICoverageDefinition->GetGraphics();
```
 
说    明：pICoverageDefinition为ICoverageDefinition类指针

#### GetIsObjectGraphicsVisible()

函数说明：获得对象是否在二维显示

返回参数类型：`bool`

返回参数说明：对象是否在二维显示

举    例：
```cpp
bool bIsObjectGraphicsVisible = pICvGraphics->GetIsObjectGraphicsVisible();
```
 
#### SetIsObjectGraphicsVisible()

函数说明：设置对象是否在二维显示

输入类型：`bool`

输入说明：对象是否在二维显示

举    例：
```cpp
pICvGraphics->SetIsObjectGraphicsVisible(bIsObjectGraphicsVisible);
```
 
#### GetIsRegionVisible()

函数说明：获得对象是否显示区域

返回参数类型：`bool`

返回参数说明：对象是否显示区域

举    例：
```cpp
bool bIsRegionVisible = pICvGraphics->GetIsRegionVisible();
```
 
#### SetIsRegionVisible()

函数说明：设置对象是否显示区域

输入类型：`bool`

输入说明：对象是否显示区域

举    例：
```cpp
pICvGraphics->SetIsRegionVisible(bIsRegionVisible);
```
 
#### GetIsPointsVisible()

函数说明：获得对象是否显示点

返回参数类型：`bool`

返回参数说明：对象是否显示点

举    例：
```cpp
bool bIsPointsVisible = pICvGraphics->GetIsPointsVisible();
```
 
#### SetIsPointsVisible()

函数说明：设置对象是否显示点

输入类型：`bool`

输入说明：对象是否显示点

举    例：
```cpp
pICvGraphics->SetIsPointsVisible(bIsPointsVisible);
```
 
#### GetColor()

函数说明：获得二维显示颜色

返回参数类型：`int`

返回参数说明：二维显示颜色

举    例：
```cpp
int nColor = pICvGraphics->GetColor();
```
 
#### SetColor()

函数说明：设置二维显示颜色

输入类型：`int`

输入说明：二维显示颜色

举    例： 
```cpp
pICvGraphics->SetColor(nColor);
```
 
#### GetPointSize()

函数说明：获得二点大小

返回参数类型：`double`

返回参数说明：点大小

举    例：
```cpp
double dPointSize = pICvGraphics->GetPointSize();
```

#### SetPointSize()

函数说明：设置点大小

输入类型：`double`

输入说明：点大小

举    例：
```cpp
pICvGraphics->SetPointSize(dPointSize);
```

#### GetFillPoints()

函数说明：获得对象是否填充

返回参数类型：`bool`

返回参数说明：对象是否填充

举    例：
```cpp
bool bFillPoints = pICvGraphics->GetFillPoints();
```

#### SetFillPoints()

函数说明：设置对象是否填充

输入类型：`bool`

输入说明：对象是否填充

举    例：
```cpp
pICvGraphics->SetFillPoints(bFillPoints);
```
