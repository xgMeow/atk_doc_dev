# 火箭类

## 类 ILaunchVehicle 
 
> [!info]
> 类 ILaunchVehicle，继承自类IAtkObject

属性类型：火箭

举    例：
```cpp
ILaunchVehicle* pILaunchVehicle = (ILaunchVehicle*)pIScen->GetChildren()->New(eLaunchVehicle, "ILaunchVehicle");
```

### GetTrajectoryType()

函数说明：获得火箭轨道类型

返回参数类型：`EVePropagatorType`

返回参数说明：火箭轨道类型

举    例：
```cpp
EVePropagatorType eTrajectoryType = pIlaunchVehicle->GetTrajectoryType();
```

### SetTrajectoryType()

函数说明：设置火箭轨道类型

输入类型：`EVePropagatorType`

输入说明：火箭轨道类型

举    例：
```cpp
pIlaunchVehicle->SetTrajectoryType (eTrajectoryType);
```

### GetTrajectorySupportedTypes()

函数说明：获得支持的轨道类型

返回参数类型：`std::vector<std::string>`

返回参数说明：支持的轨道类型

举    例：
```cpp
std::vector<std::string> supTypeList = pIlaunchVehicle->GetTrajectorySupportedTypes();
```

### IsTrajectoryTypeSupported()

函数说明：判断是否支持轨道类型

输入类型：`EVePropagatorType`

输入说明：轨道类型

返回参数类型：`bool`

返回参数说明：是否支持轨道类型

举    例：
```cpp
bool isSupported = pILaunchVehicle->IsTrajectoryTypeSupported();
```

### GetTrajectory()

函数说明：获得轨道指针

返回参数类型：`IVePropagator*`

返回参数说明：轨道指针

举    例：
```cpp
IVePropagator* pIVePropagator = pIlaunchVehicle->GetTrajectory();
```

### GetGraphics()

函数说明：获得二维属性

返回参数类型：`ILvGraphics*`

返回参数说明：二维属性指针

举    例：
```cpp
ILVGraphics* pILVGraphics = pILaunchVehicle->GetGraphics();
```

### GetVO()

函数说明：获得三维属性

返回参数类型：`ILVVO*`

返回参数说明：三维属性指针

举    例：
```cpp
ILVVO* pILVVO = pILaunchVehicle->GetVO();
```

## 类IVePropagator 

## 类 IVePropagatorSimpleAscent
 
> [!info]
> 类 IVePropagatorSimpleAscent，继承自类IVePropagator

属性类型：`IVePropagatorSimpleAscent*`

属性说明：轨道类型：简单上升预报器

举    例：
```cpp
IVePropagator * pIVePropagator = pIlaunchVehicle->GetTrajectory();

IVePropagatorSimpleAscent* pIVePropagatorSimpleAscent = (IVePropagatorSimpleAscent *) pIVePropagator;
```

### GetEphemerisInterval()

函数说明：获得轨道传播间隔

返回参数类型：`ICrdnEventIntervalSmartInterval*`

返回参数说明：轨道传播间隔

举    例：
```cpp
ICrdnEventIntervalSmartInterval* pICrdnEventIntervalSmartInterval = pIVePropagatorSimpleAscent->GetEphemerisInterval();
```

### GetInitialState()

函数说明：获得轨道状态指针

返回参数类型：`IVeLVInitialState*`

返回参数说明：轨道状态指针

举    例：
```cpp
IVeLVInitialState* pIVeLVInitialState = pIVePropagatorSimpleAscent->GetInitialState();
```

### GetStep()

函数说明：获得轨道步长

返回参数类型：`double`

返回参数说明：轨道步长

举    例：
```cpp
double dStep = pIVePropagatorSimpleAscent->GetStep();
```

### SetStep()

函数说明：设置轨道步长

输入类型：`double`

输入说明：轨道步长

举    例：
```cpp
pIVePropagatorSimpleAscent->SetStep(dStep);
```

## 类 IVePropagatorStkExternal
 
> [!info]
> 类 IVePropagatorStkExternal，继承自类IVePropagator

属性类型：`IVePropagatorStkExternal*`

属性说明：轨道类型：STK星历

举    例：
```cpp
IVePropagator * pIVePropagator = pIlaunchVehicle->GetTrajectory();

IVePropagatorStkExternal* pIVePropagatorStkExternal= (IVePropagatorStkExternal*) pIVePropagator;
```

### GetStartTime()

函数说明：获得开始时间

返回参数类型：`std::string`

返回参数说明：开始时间

举    例：
```cpp
std::string startTime = pIVePropagatorStkExternal->GetStartTime();
```

### GetStopTime()

函数说明：获得结束时间

返回参数类型：`std::string`

返回参数说明：结束时间

举    例：
```cpp
std::string stopTime = pIVePropagatorStkExternal->GetStopTime();
```

### SetFilename()

函数说明：设置STK星历文件路径

输入类型：`std::string`

输入说明：STK星历文件路径

举    例：
```cpp
pIVePropagatorStkExternal->SetFilename(strFilename); 
```

### GetFilename()

函数说明：获得STK星历文件路径

返回参数类型：`std::string`

返回参数说明：STK星历文件路径

举    例：
```cpp
std::string strFilename = pIVePropagatorStkExternal->GetFilename();
```

## 类ICrdnEventIntervalSmartInterval

举    例：
```cpp
ICrdnEventIntervalSmartInterval* pICrdnEventIntervalSmartInterval = pIVePropagatorSimpleAscent->GetEphemerisInterval();
```

说    明：pIVePropagatorSimpleAscent为IVePropagatorSimpleAscent类指针

### SetStartEpoch()

函数说明：设置开始时间

输入类型：`std::string`

输入说明：开始时间

举    例：
```cpp
pICrdnEventIntervalSmartInterval->SetStartEpoch(startEpoch); 
```

### GetStartEpoch()

函数说明：获得开始时间

返回参数类型：`std::string`

返回参数说明：开始时间

举    例：
```cpp
std::string startEpoch = pICrdnEventIntervalSmartInterval->GetStartEpoch(); 
```

### SetStopEpoch()

函数说明：设置结束时间

输入类型：`std::string`

输入说明：结束时间

举    例：
```cpp
pICrdnEventIntervalSmartInterval->SetStopEpoch(stopEpoch);  
```

### GetStopEpoch()

函数说明：获得结束时间

返回参数类型：`std::string`

返回参数说明：结束时间

举    例：
```cpp
std::string stopEpoch = pICrdnEventIntervalSmartInterval->GetStopEpoch();
```

## 类IVeLvInitialState 

举    例：
```cpp
IVeLvInitialState* pIVeLvInitialState = pIVePropagatorSimpleAscent->GetInitialState();
```

说    明：pIVePropagatorSimpleAscent为IVePropagatorSimpleAscent类指针

### GetLaunch()

函数说明：获得发射点LLA

返回参数类型：`ILLAPosition*`

返回参数说明：发射点LLA

举    例：
```cpp
ILLAPosition* pILLAPosition = pIVeLvInitialState->GetLaunch();
```

### GetBurnout()

函数说明：获得发射终点LLA

返回参数类型：`ILLAPosition*`

返回参数说明：发射终点LLA

举    例：
```cpp
ILLAPosition* pILLAPosition = pIVeLvInitialState->GetBurnout();
```

### GetBurnoutVel()

函数说明：获得发射终点速度大小(m/s)

返回参数类型：`double`

返回参数说明：发射终点速度大小(m/s)

举    例：
```cpp
double dBurnout = pIVeLvInitialState->GetBurnoutVel();
```

### SetBurnoutVel()

函数说明：设置发射终点速度大小(m/s)

输入类型：`double`

输入说明：发射终点速度大小(m/s)

举    例：
```cpp
pIVeLvInitialState->SetBurnoutVel(dBurnout);
```

## 类ILLAPosition

举    例：
```cpp
ILLAPosition* pILLAPosition = pIVeLvInitialState->GetLaunch();
```

说    明：pIVeLvInitialState为IVeLvInitialState类指针

### Assign()

函数说明：分配坐标

输入类型：`ILLAPosition*`

输入说明：位置信息

举    例：
```cpp
pILLAPosition->Assign(pILLAPosition);
```

### AssignGeocentric()

函数说明：设置地理坐标

输入类型：`double`, `double`, `double`

输入说明：lat 地理位置纬度(rad), lon 地理位置经度(rad) ,alt 地理位置高度(m)

举    例：
```cpp
pILLAPosition->AssignGeocentric (0.1, 0.2, 3000);
```

### AssignGeodetic()

函数说明：设置地理坐标

输入类型：`double`, `double`, `double`

输入说明：lat 地理位置纬度(deg), lon 地理位置经度(deg) ,alt 地理位置高度(m)

举    例：
```cpp
pILLAPosition->AssignGeodetic (28, 112, 5000);
```

### ConvertTo()

函数说明：转换坐标类型

输入类型：`ELLAPositionType`

输入说明：坐标类型

输出类型：`ILLAPosition*`

输出说明：转换后类型指针

举    例：
```cpp
ILLAPosition* pILLAPosition = pILLAPosition->ConvertTo(converType);
```

### GetType()

函数说明：获得轨道类型

返回参数类型：`ELLAPositionType`

返回参数说明：轨道类型

举    例：
```cpp
ELLAPositionType type = pILLAPosition->GetType();
```

## 火箭二维

### 类ILvGraphics

举    例：
```cpp
ILvGraphics * pILvGraphics = pIlaunchVehicle->GetGraphics();
```

说    明：pIlaunchVehicle为IlaunchVehicle类指针

#### IsAttributesTypeSupported()

函数说明：判断二维显示是否支持此类型

输入类型：`EVeGfxAttributes`

输入说明：二维显示类型

返回参数类型：`bool`

返回参数说明：二维显示是否支持此类型

举    例：
```cpp
bool isSupported = pILvGraphics->IsAttributesTypeSupported(eAttributes); 
```

#### SetAttributesType()

函数说明：设置二维显示类型

输入类型：`EVeGfxAttributes`

输入说明：二维显示类型

举    例：
```cpp
pILvGraphics->SetAttributesType(eAttributes); 
```

#### GetAttributes()

函数说明：获得二维显示类型属性

返回参数类型：`IVeGfxAttributes*`

返回参数说明：二维显示类型指针

举    例：
```cpp
IVeGfxAttributes* pIVeGfxAttributes = pILvGraphics->GetAttributes();
```

#### GetAttributesSupportedTypes()

函数说明：获得二维显示支持类型

返回参数类型：`std::vector<std::string>`

返回参数说明：二维显示支持类型

举    例：
```cpp
std::vector<std::string> supTypeList = pILvGraphics->GetAttributesSupportedTypes();
```

#### GetAttributesType()

函数说明：获得二维显示类型

返回参数类型：`EVeGfxAttributes`

返回参数说明：二维显示类型

举    例：
```cpp
EVeGfxAttributes eVeGfxAttributes = pILvGraphics->GetAttributesType();
```

#### GetPassData()

函数说明：获得二维属性指针

返回参数类型：`IVeGfxTrajectoryPassData*`

返回参数说明：二维属性指针

举    例：
```cpp
IVeGfxTrajectoryPassData* pIVeGfxTrajectoryPassData = pILvGraphics->GetPassData();
```

### 类IveGfxTrajectoryPassData 

举    例：
```cpp
IveGfxTrajectoryPassData* pIveGfxTrajectoryPassData = pILvGraphics->GetPassData();
```

说    明：pILvGraphics为ILvGraphics类指针

#### GetTrajectory()

函数说明：获得轨迹属性

返回参数类型：`IVeGfxLeadTrailData*`

返回参数说明：轨迹属性

举    例：
```cpp
IVeGfxLeadTrailData* pIVeGfxLeadTrailData = pIveGfxTrajectoryPassData->GetTrajectory();
```

## 火箭三维

### 类ILvVO

举    例：
```cpp
ILvVO* pILvVO = pIlaunchVehicle->GetVO();
```

说    明：pIlaunchVehicle为IlaunchVehicle类指针

#### GetOffsets()

函数说明：获得三维模型属性

返回参数类型：`IVOOffset*`

返回参数说明：三维模型属性指针

举    例：
```cpp
IVOOffset* pIVOOffset = pILvVO->GetOffsets();
```

#### GetVector()

函数说明：获得三维向量属性

返回参数类型：`IVOVector*`

返回参数说明：三维向量属性指针

举    例：
```cpp
IVOVector* pIVOVector = pILvVO->GetVector();
```

#### GetTrajectorySystems()

函数说明：获得三维轨道系统指针

返回参数类型：`IVeVOSystemsCollection*`

返回参数说明：三维轨道系统指针

举    例：
```cpp
IVeVOSystemsCollection* pIVeVOSystemsCollection = pILvVO->GetTrajectorySystems();
```
