# 车辆类

## 类 IGroundVehicle
 
> [!info]
> 类 IGroundVehicle, 继承自类IAtkObject

属性类型：车辆

举  例 ：
```cpp
IGroundVehicle*pIGroundVehicle  = (IGroundVehicle*)plScen->GetChildren()->New(eGroundVehicle,"IGroundVehicle");
```
 
### GetGreatArcVehicle()

函数说明：获得车辆轨道预报器

返回参数类型： `IGreatArcVehicle*`

返回参数说明：轨道预报器

举  例 ：
```cpp
IGreatArcVehicle* pIGreatArcVehicle = pIGroundVehicle->GetGreatArcVehicle();
```


### GetGraphics()

函数说明：获得二维属性

返回参数类型：`IGvGraphics*`

返回参数说明：二维属性指针

举    例：
```cpp
IGvGraphics * pIGvGraphics = pIGroundVehicle->GetGraphics();
```
 
### GetVO()

函数说明：获得三维属性

返回参数类型：`IGvVO*`

返回参数说明：三维属性指针

举    例：
```cpp
IGvVO* pIGvVO = pIGroundVehicle->GetVO();
```
 
## 车辆二维

### 类 IGvGraphics

> [!info]
> 类 IGvGraphics，继承自类IGreatArcGraphics

举    例：
```cpp
IGvGraphics* pIGvGraphics = pIGroundVehicle->GetGraphics();
```
 
说    明：pIGroundVehicle为IGroundVehicle类指针

## 车辆三维

### 类IGvVO

举    例：
```cpp
IGvVO * pIGvVO = pIGroundVehicle->GetVO();
```
 
说    明：pIGroundVehicle为IGroundVehicle类指针

#### GetOffsets()

函数说明：获得三维模型属性

返回参数类型：`IVOOffset*`

返回参数说明：三维模型属性指针

举    例：
```cpp
IVOOffset* pIVOOffset = pIGvVO->GetOffsets();
```
 
#### GetVector()

函数说明：获得三维向量属性

返回参数类型：`IVOVector*`

返回参数说明：获得三维向量属性

举    例：
```cpp
IVOVector* pIVOVector = pIGvVO->GetVector();
```
 