# 接收器类

## 类 IReceiver 
 
> [!info]
> 类 IReceiver ，继承自类 IAtkObject

属性类型： 接收器

举  例： 
```cpp
IReceiver* pIReceiver = (IReceiver*)pIFacility->GetChildren()->New(eReceiver, "IReceiver");
```

###  SetModel()
 
函数说明： 按名称设置当前接收模型

输入类型： `std::string`

输入说明： 接收模型名称(简单模型： "Simple Receiver Model")

举  例： 
```cpp
pIReceiver->SetModel("Simple Receiver Model");
```
  
###  GetModel() 
 
函数说明：获得接收模型

返回参数类型： `IReceiverModel*`

返回参数说明： 接收模型

举  例： 
```cpp
IReceiverModel *pIReceiverModel = pIReceiver->GetModel(); 
``` 

## 类 IReceiverModel
 
### GetName() 
 
函数说明：获得模型名称

返回参数类型： `std:string`

返回参数说明：模型名称

举  例： 
```cpp
plReceiverModel>GetName(); 
```
  
### GetType() 
 
函数说明：获得接收模型类型

返回参数类型： `EreceiverModelType`

返回参数说明：接收模型类型

举  例： 
```cpp
plIReceiverModel->GetType(); 
```
  
## 类 IReceiverModelSimple
 
> [!info]
> 类 IReceiverModelSimple, 继承自类 IReceiverModel

属性类型： `IReceiverModelSimple*`

属性说明：模型子类：简单

举  例 ：
```cpp
plReceiver->SetModel("Simple Receiver Model");

IReceiverModelSimple*plReceiverModelSimple=(IReceiverModelSimple*)pIReceiver->GetModel();
```
  
### GetFrequency()
 
函数说明：获得频率(GHz)

返回参数类型：`double`

返回参数说明：频率(GHz)

举  例： 
```cpp
plReceiverModelSimple>GetFrequency(); 
``` 

### SetFrequency()

函数说明： 设置频率(扩展函数)

输入类型： `double`

输入说明： 频率

举  例： 
```cpp
pIReceiverModelSimple->SetFrequency(15.5);
```
  
###  GetGoverT() 
 
函数说明：获得品质因子(dB/k)

返回参数类型： `double`

返回参数说明： 品质因子(dB/k)

举  例： 
```cpp
pIReceiverModelSimple->GetGOverT(); 
```
  
###  SetGOverT()
 
函数说明： 设置品质因子(扩展函数)

输入类型： `double`

输入说明： 品质因子

举  例： 
```cpp
pIReceiverModelSimple->SetGOverT(2);
```
  
###  SetPolarizationType()
 
函数说明： 设置天线极化类型 (子类之间类型转换，子类成员值不变)

输入类型： `EPolarizationType`

输入说明： 天线极化类型

举  例： 
```cpp
pIReceiverModelSimple->SetPolarizationType (ePolarizationTypeNone);
```
  
###  GetPolarization() 
 
函数说明：获得线极化

返回参数类型： `IPolarization*`

返回参数说明： 线极化

举  例： 
```cpp
IPolarization* pIPolarization = pIReceiverModelSimple->GetPolarization(); 
``` 
 
###  GetLinkMargin() 

函数说明：获得路裕量

返回参数类型： `ILinkMargin*`

返回参数说明：路裕量

举  例： 
```cpp
plReceiverModelSimple->GetLinkMargin(); 
```
  
### SetDemodulator()
 
函数说明：按名称设置当前解调器模型

输入类型： `std:string`

输入说明：解调器模型名称

举  例 ：
```cpp
plReceiverModelSimple->SetDemodulator("BPSK");
```
  
### GetDemodulator()
 
函数说明：获得解调器

返回参数类型： `IDemodulatorModel*`

返回参数说明：解调器

举  例： 
```cpp
IDemodulatorModel*pIDemodulatorModel  =plReceiverModelSimple->GetDemodulator(); 
```
  
## 类IPolarization
 
### GetType()
 
函数说明：获得天线极化类型

返回参数类型：`EPolarizationType`

返回参数说明：天线极化类型

举  例： 
```cpp
plPolarization->GetType(); 
```
  
## 类 IPolarizationNone

> [!info]
> 类 IPolarizationNone, 继承自类Ipolarization

属性类型： `IPolarizationNone*`

属性说明：天线极化子类： None

举  例：
```cpp
plReceiverModelSimple->SetPolarizationType(ePolarizationTypeNone);

IPolarizationNone* pIPolarizationNone = (IPolarizationNone*)pIReceiverModelSimple->GetPolarization();
```
 
###  GetReferenceAxis() 
  
函数说明：获得天线极化参考轴

返回参数类型： `EPolarizationReferenceAxis`

返回参数说明： 天线极化参考轴

举  例： 
```cpp
pIPolarizationNone->GetReferenceAxis(); 
```
  
###  SetReferenceAxis()
 
函数说明： 设置天线极化参考轴

输入类型： `EpolarizationReferenceAxis`

输入说明： 天线极化参考轴

举  例：
```cpp
pIPolarizationNone->SetReferenceAxis(ePolarizationReferenceAxisX);
```
  
###  GetTiltAngle() 
 
函数说明：获得极化角度(deg)

返回参数类型： `double`

返回参数说明： 极化角度(deg)

举  例： 
```cpp
pIPolarizationNone->GetTiltAngle(); 
``` 

###  SetTiltAngle()
 
函数说明： 设置极化角度(deg)

输入类型： `double`

输入说明： 极化角度(deg)

举  例： 
```cpp
pIPolarizationNone->SetTiltAngle(2);
```
  
## 类 IPolarizationLinear  
  
> [!info]
> 类 IPolarizationLinear ，继承自类 IPolarization
 
属性类型： `IPolarizationLinear*`

属性说明： 天线极化子类： 线性


举  例：
```cpp
pIReceiverModelSimple->SetPolarizationType(ePolarizationTypeLinear);

IPolarizationLinear* pIPolarizationLinear = (IPolarizationLinear*)pIReceiverModelSimple->GetPolarization();
```
  
###  GetReferenceAxis() 

函数说明：获得天线极化参考轴

返回参数类型： `EPolarizationReferenceAxis`

返回参数说明： 天线极化参考轴

举  例： 
```cpp
pIPolarizationLinear->GetReferenceAxis(); 
``` 

###  SetReferenceAxis()
 
函数说明： 设置天线极化参考轴

输入类型： `EpolarizationReferenceAxis`

输入说明： 天线极化参考轴

举  例：
```cpp
pIPolarizationLinear->SetReferenceAxis(ePolarizationReferenceAxisX);
```
  
###  GetTiltAngle() 
 
函数说明：获得极化角度(deg)

返回参数类型： `double`

返回参数说明： 极化角度(deg)

举  例： 
```cpp
pIPolarizationLinear->GetTiltAngle(); 
```

###  SetTiltAngle()
 
函数说明： 设置极化角度(deg)

输入类型： `double`

输入说明： 极化角度(deg)

举  例： 
```cpp
pIPolarizationLinear->SetTiltAngle(2);
```
  
## 类 IPolarizationCircular
 
> [!info]
> 类 IPolarizationCircular ，继承自类 Ipolarization
 
属性类型： `IPolarizationCircular*`

属性说明： 天线极化子类： 循环

举  例：
```cpp
pIReceiverModelSimple->SetPolarizationType(ePolarizationTypeCircular);

IPolarizationCircular* pIPolarizationCircular = (IPolarizationCircular*)pIReceiverModelSimple->GetPolarization();
```
  
###  GetReferenceAxis() 
 

函数说明：获得天线极化参考轴

返回参数类型： `EPolarizationReferenceAxis`

返回参数说明： 天线极化参考轴

举  例： 
```cpp
pIPolarizationCircular->GetReferenceAxis(); 
```
  
###  SetReferenceAxis()
 
函数说明： 设置天线极化参考轴

输入类型： `EpolarizationReferenceAxis`

输入说明： 天线极化参考轴

举  例：
```cpp
pIPolarizationCircular->SetReferenceAxis(ePolarizationReferenceAxisX);
``` 

###  GetTiltAngle() 
 
函数说明：获得极化角度(deg)

返回参数类型： `double`

返回参数说明： 极化角度(deg)

举  例： 
```cpp
pIPolarizationCircular->GetTiltAngle(); 
```
  
###  SetTiltAngle()
 
函数说明： 设置极化角度(deg)

输入类型： `double`

输入说明： 极化角度(deg)

举  例： 
```cpp
pIPolarizationCircular->SetTiltAngle(2);
```

## 类 IPolarizationVertical 
  
> [!info]
> 类 IPolarizationVertical ，继承自类 Ipolarization

属性类型： `IPolarizationVertical*`

属性说明： 天线极化子类： 垂直

举  例：
```cpp
pIReceiverModelSimple->SetPolarizationType(ePolarizationTypeVertical);

IPolarizationVertical* pIPolarizationVertical = (IPolarizationVertical*)pIReceiverModelSimple->GetPolarization();
```
  
###  GetReferenceAxis() 
 
函数说明：获得天线极化参考轴

返回参数类型： `EPolarizationReferenceAxis`

返回参数说明： 天线极化参考轴

举  例： 
```cpp
pIPolarizationVertical->GetReferenceAxis(); 
```
  
###  SetReferenceAxis()
 
函数说明： 设置天线极化参考轴

输入类型： `EpolarizationReferenceAxis`

输入说明： 天线极化参考轴

举  例：
```cpp
pIPolarizationVertical->SetReferenceAxis(ePolarizationReferenceAxisX);
```
  
###  GetTiltAngle() 
 
函数说明：获得极化角度(deg)

返回参数类型： `double`

返回参数说明： 极化角度(deg)

举  例： 
```cpp
pIPolarizationVertical->GetTiltAngle(); 
```
  
###  SetTiltAngle()
  
函数说明： 设置极化角度(deg)

输入类型： `double`

输入说明： 极化角度(deg)
 
举  例： 
```cpp
pIPolarizationVertical->SetTiltAngle(2)
```
  
## 类 IPolarizationHorizontal
  
> [!info]
> 类 IPolarizationHorizontal ，继承自类 IPolarization

属性类型： `IPolarizationLinear*`

属性说明： 天线极化子类： 水平

举  例：
```cpp
pIReceiverModelSimple->SetPolarizationType(ePolarizationTypeHorizontal);

IPolarizationHorizontal* pIPolarizationHorizontal = (IPolarizationHorizontal*)pIReceiverModelSimple->GetPolarization();
```
  
###  GetReferenceAxis() 
 
函数说明：获得天线极化参考轴

返回参数类型： `EPolarizationReferenceAxis`

返回参数说明： 天线极化参考轴

举  例： 
```cpp
pIPolarizationHorizontal->GetReferenceAxis(); 
```

###  SetReferenceAxis()

函数说明： 设置天线极化参考轴

输入类型： `EpolarizationReferenceAxis`

输入说明： 天线极化参考轴

举  例：
```cpp
pIPolarizationHorizontal->SetReferenceAxis(ePolarizationReferenceAxisX);
```
  
###  GetTiltAngle() 
 
函数说明：获得极化角度(deg)

返回参数类型： `double`

返回参数说明： 极化角度(deg)

举  例： 
```cpp
pIPolarizationHorizontal->GetTiltAngle(); 
```

###  SetTiltAngle()
 
函数说明： 设置极化角度(deg)
 
输入类型： `double`

输入说明： 极化角度(deg)

举  例： 
```cpp
pIPolarizationHorizontal->SetTiltAngle(2)
```
  
## 类 IPolarizationElliptical 
 
> [!info]
> 类 IPolarizationElliptical ，继承自类 IPolarization

属性类型： `IPolarizationElliptical*`

属性说明： 天线极化子类： 椭圆

举  例：
```cpp
pIReceiverModelSimple->SetPolarizationType(ePolarizationTypeElliptical);

IPolarizationElliptical* pIPolarizationElliptical = (IPolarizationElliptical*)pIReceiverModelSimple->GetPolarization();
```
  
###  GetReferenceAxis() 
 
函数说明：获得天线极化参考轴

返回参数类型： `EPolarizationReferenceAxis`

返回参数说明： 天线极化参考轴

举  例： 
```cpp
pIPolarizationElliptical->GetReferenceAxis(); 
```
  
###  SetReferenceAxis()
 
函数说明： 设置天线极化参考轴

输入类型： `EpolarizationReferenceAxis`

输入说明： 天线极化参考轴

举  例：
```cpp
pIPolarizationElliptical->SetReferenceAxis(ePolarizationReferenceAxisX);
```
  
###  GetTiltAngle() 
 
函数说明：获得极化角度(deg)

返回参数类型： `double`

返回参数说明： 极化角度(deg)

举  例： 
```cpp
pIPolarizationElliptical->GetTiltAngle(); 
```
  
###  SetTiltAngle()
 
函数说明：设置极化角度(deg)

输入类型： `double`

输入说明：极化角度(deg)

举  例： 
```cpp
plPolarizationElliptical>SetTiltAngle(2)
```

## 类ILinkMargin
 
### GetType() 
 
函数说明：获得链路裕量类型

返回参数类型： `ELinkMarginType`

返回参数说明：链路裕量类型

举  例： 
```cpp
plLinkMargin->GetType(); 
```
  
### SetType()
 
函数说明：设置链路裕量类型(扩展函数)

输入类型： `ElinkMarginType`

输入说明：链路裕量类型

举  例： 
```cpp
plLinkMargin->SetType(eLinkMarginTypeRip);
``` 

### GetThreshold() 
 
函数说明：获得阈值

返回参数类型： `double`

返回参数说明：阈值

举  例： 
```cpp
plLinkMargin->GetThreshold(); 
```
  
### SetThreshold()
 
函数说明：设置阈值(扩展函数)

输入类型： `double`

输入说明：阈值

举  例 ：
```cpp
plLinkMargin->SetThreshold(2);
```
  
## 类IDemodulatorModel

### GetName() 
 
函数说明：获得模型名称

返回参数类型： `std:string`

返回参数说明：模型名称

举  例： 
```cpp
plDemodulatorModel>GetName(); 
```
  
### GetType() 
 
函数说明：获得模型类型

返回参数类型： `EDemodulatorModelType`

返回参数说明：模型类型

举  例： 
```cpp
pIDemodulatorModel->GetType(); 
```

## 类 IDemodulatorModelBpsk, 继承自类 IDemodulatorModel

> [!note]
> 待补充

## 类IDemodulatorModelQpsk, 继承自类IdemodulatorModel

> [!note]
> 待补充
