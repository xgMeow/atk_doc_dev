# 高级接近分析类

## 类 IAdvCAT 
 
> [!info]
> 类 IAdvCAT，继承自类IAtkObject

属性类型：高级接近分析对象

举    例：
```cpp
IAdvCAT* pIAdvCAT = (IAdvCAT*)pIScen->GetChildren()->New(eAdvCAT, "IAdvCAT");
```

### GetMain()

函数说明：获得高级接近分析主配置操作指针

返回参数类型：`IAdvCATMain*`

返回参数说明：高级接近分析主配置操作指针

举    例：
```cpp
IAdvCATMain* pIAdvCATMain = pIAdvCAT->GetMain();
```

### GetAdvance()

函数说明：获得高级接近分析高级配置操作指针

返回参数类型：`IAdvCATAdvance*`

返回参数说明：高级接近分析高级配置操作指针

举    例：
```cpp
IAdvCATAdvance* pIAdvCATAdvance = pIAdvCAT->GetAdvance();
```

## 类IAdvCATMain 

属性类型：高级接近分析主配置操作

举    例：
```cpp
IAdvCATMain* pIAdvCATMain = pIAdvCAT->GetMain();
```

### SetPrimaryList()

函数说明：设置主目标序列

输入类型：`ISatellite*`

输入说明：主目标对象

举    例：
```cpp
pIAdvCATMain->SetPrimaryList(pISatellite);
```

### GetPrimaryList()

函数说明：获取主目标序列

返回参数类型：`std::vector<ISatellite*>`

返回参数说明：主目标列表

举    例：
```cpp
std::vector<ISatellite*> vecSate = pIAdvCATMain->GetPrimaryList();
```

### RemovePrimary()

函数说明：从主目标列表移除指定目标对象

输入类型：`ISatellite*`

输入说明：主目标对象

举    例：
```cpp
pIAdvCATMain->RemovePrimary(pISatellite);
```

### RemoveAllPrimary()

函数说明：清空主目标列表

举    例：
```cpp
pIAdvCATMain->RemoveAllPrimary();
```

### SetPriTangential()

函数说明：设置椭球三轴切向

输入类型：`ISatellite*, double`

输入说明：主目标对象，椭球三轴切向

举    例：
```cpp
pIAdvCATMain->SetPriTangential(pISatellite, 2000);
```

### GetPriTangential()

函数说明：获取椭球三轴切向

输入类型：`ISatellite*`

输入说明：主目标对象

返回参数类型：`double`

返回参数说明：椭球三轴切向

举    例：
```cpp
double dValue = pIAdvCATMain->GetPriTangential(pISatellite);
```

### SetPriCrossTrack()

函数说明：设置椭球三轴横向

输入类型：`ISatellite*, double`

输入说明：主目标对象，椭球三轴横向

举    例：
```cpp
pIAdvCATMain->SetPriCrossTrack(pISatellite, 1000);
```

### GetPriCrossTrack()

函数说明：获取椭球三轴横向

输入类型：`ISatellite*`

输入说明：主目标对象

返回参数类型：`double`

返回参数说明：椭球三轴横向

举    例：
```cpp
double dValue = pIAdvCATMain->GetPriCrossTrack(pISatellite);
```

### SetPriNormal()

函数说明：设置椭球三轴法向

输入类型：`ISatellite*, double`

输入说明：主目标对象，椭球三轴法向

举    例：
```cpp
pIAdvCATMain->SetPriNormal(pISatellite, 500);
```

### GetPriNormal()

函数说明：获取椭球三轴法向

输入类型：`ISatellite*`

输入说明：主目标对象

返回参数类型：`double`

返回参数说明：椭球三轴法向

举    例：
```cpp
double dValue = pIAdvCATMain->GetPriNormal(pISatellite);
```

### SetPriRadius()

函数说明：设置包络球半径

输入类型：`ISatellite*, double`

输入说明：主目标对象，包络球半径

举    例：
```cpp
pIAdvCATMain->SetPriRadius(pISatellite, 1);
```

### GetPriRadius()

函数说明：获取包络球半径

输入类型：`ISatellite*`

输入说明：主目标对象

返回参数类型：`double`

返回参数说明：包络球半径

举    例：
```cpp
double dValue = pIAdvCATMain->GetPriRadius(pISatellite);
```

### SetPriClass()

函数说明：设置椭球类别

输入类型：`ISatellite*, EAdvCatEllipType`

输入说明：主目标对象，椭球类别

举    例：
```cpp
pIAdvCATMain->SetPriClass(pISatellite, eFixed);
```

### GetPriClass()

函数说明：获取椭球类别

输入类型：`ISatellite*`

输入说明：主目标对象

返回参数类型：`EAdvCatEllipType`

返回参数说明：椭球类别

举    例：
```cpp
EAdvCatEllipType eClassType = pIAdvCATMain->GetPriClass(pISatellite);
```

### SetSecondList()

函数说明：设置次目标序列

输入类型：`ISatellite*`

输入说明：次目标对象

举    例：
```cpp
pIAdvCATMain->SetSecondList(pISatellite);
```

### GetSecondList()

函数说明：获取次目标序列

返回参数类型：`std::vector<ISatellite*>`

返回参数说明：次目标列表

举    例：
```cpp
std::vector<ISatellite*> vecSate = pIAdvCATMain->GetSecondList();
```

### RemoveSecond()

函数说明：从次目标列表移除指定目标对象

输入类型：`ISatellite*`

输入说明：次目标对象

举    例：
```cpp
pIAdvCATMain->RemoveSecond(pISatellite);
```

### RemoveAllSecond()

函数说明：清空次目标列表

举    例：
```cpp
pIAdvCATMain->RemoveAllSecond();
```

### SetScdTangential()

函数说明：设置椭球三轴切向

输入类型：`ISatellite*, double`

输入说明：次目标对象，椭球三轴切向

举    例：
```cpp
pIAdvCATMain->SetScdTangential(pISatellite, 2000);
```

### GetScdTangential()

函数说明：获取椭球三轴切向

输入类型：`ISatellite*`

输入说明：次目标对象

返回参数类型：`double`

返回参数说明：椭球三轴切向

举    例：
```cpp
double dValue = pIAdvCATMain->GetScdTangential(pISatellite);
```

### SetScdCrossTrack()

函数说明：设置椭球三轴横向

输入类型：`ISatellite*, double`

输入说明：次目标对象，椭球三轴横向

举    例：
```cpp
pIAdvCATMain->SetScdCrossTrack(pISatellite, 1000);
```

### GetScdCrossTrack()

函数说明：获取椭球三轴横向

输入类型：`ISatellite*`

输入说明：次目标对象

返回参数类型：`double`

返回参数说明：椭球三轴横向

举    例：
```cpp
double dValue = pIAdvCATMain->GetScdCrossTrack(pISatellite);
```

### SetScdNormal()

函数说明：设置椭球三轴法向

输入类型：`ISatellite*, double`

输入说明：次目标对象，椭球三轴法向

举    例：
```cpp
pIAdvCATMain->SetScdNormal(pISatellite, 500);
```

### GetScdNormal()

函数说明：获取椭球三轴法向

输入类型：`ISatellite*`

输入说明：次目标对象

返回参数类型：`double`

返回参数说明：椭球三轴法向

举    例：
```cpp
double dValue = pIAdvCATMain->GetScdNormal(pISatellite);
```

### SetScdRadius()

函数说明：设置包络球半径

输入类型：`ISatellite*, double`

输入说明：次目标对象，包络球半径

举    例：
```cpp
pIAdvCATMain->SetScdRadius(pISatellite, 1);
```

### GetScdRadius()

函数说明：获取包络球半径

输入类型：`ISatellite*`

输入说明：次目标对象

返回参数类型：`double`

返回参数说明：包络球半径

举    例：
```cpp
double dValue = pIAdvCATMain->GetScdRadius(pISatellite);
```

### SetScdClass()

函数说明：设置椭球类别

输入类型：`ISatellite*, EAdvCatEllipType`

输入说明：次目标对象，椭球类别

举    例：
```cpp
pIAdvCATMain->SetScdClass(pISatellite, eFixed);
```

### GetScdClass()

函数说明：获取椭球类别

输入类型：`ISatellite*`

输入说明：次目标对象

返回参数类型：`EAdvCatEllipType`

返回参数说明：椭球类别

举    例：
```cpp
EAdvCatEllipType eClassType = pIAdvCATMain->GetScdClass(pISatellite);
```

### SetUseSceInterval()

函数说明：设置是否使用场景时间

输入类型：`bool`

输入说明：是否使用场景时间

举    例：
```cpp
pIAdvCATMain->SetUseSceInterval(true);
```

### GetUseSceInterval()

函数说明：获取是否使用场景时间

返回参数类型：`bool`

返回参数说明：是否使用场景时间

举    例：
```cpp
bool bUse = pIAdvCATMain->GetUseSceInterval();
```

### SetTimePeriod()

函数说明：设置分析时间段

输入类型：`std::string, std::string`

输入说明：开始分析时间, 结束分析时间

举    例：
```cpp
pIAdvCATMain->SetTimePeriod("1 Jan 2020 00:00:00.000", "2 Jan 2020 00:00:00.000");
```

### GetTimePeriod()

函数说明：获取分析时间段

返回参数类型：`std::string, std::string`

返回参数说明：开始分析时间, 结束分析时间

举    例：
```cpp
pIAdvCATMain->GetTimePeriod(strStartTime, strEndTime);
```

### SetThreshold()

函数说明：设置距离阈值

输入类型：`double`

输入说明：距离阈值

举    例：
```cpp
pIAdvCATMain->SetThreshold(3000);
```

### GetThreshold()

函数说明：获取距离阈值

返回参数类型：`double`

返回参数说明： 距离阈值

举    例：
```cpp
double dValue = pIAdvCATMain->GetThreshold();
```

### SetUseRangeMeasure()

函数说明：设置是否使用距离测量

输入类型：`bool`

输入说明：true = 使用点距离；false = 使用椭距

举    例：
```cpp
pIAdvCATMain->SetUseRangeMeasure(true);
```

### GetUseRangeMeasure()

函数说明：获取是否使用距离测量

返回参数类型：`bool`

返回参数说明： true = 使用点距离；false = 使用椭距

举    例：
```cpp
bool bValue = pIAdvCATMain->GetUseRangeMeasure();
```

### Compute()

函数说明：计算

举    例：
```cpp
pIAdvCATMain->Compute();
```

### GetDetail()

函数说明：获取结果数据大小

返回参数类型：`std::vector<std::string>, std::vector<std::string>, std::vector<std::string>, std::vector<double>, std::vector<double>`

返回参数说明： 主目标序列， 次目标序列， 时间， 范围， 速度

举    例：
```cpp
pIAdvCATMain->GetDetail(vecPrimID，vecsecID，vectca，veccaMinRange，veccaMinVel);
```

## 类 IAdvCATAdvance 

属性类型：高级接近分析高级配置

举    例：
```cpp
IAdvCATAdvance* pIAdvCATAdvance = pIAdvCAT->GetAdvance();
```

### SetUseDateFilter()

函数说明：设置是否使用历元日期筛选器

输入类型：`bool`

输入说明：是否使用历元日期筛选器

举    例：
```cpp
pIAdvCATAdvance->SetUseDateFilter(true);
```

### GetUseDateFilter()

函数说明：获取是否使用历元日期筛选器

返回参数类型：`bool`

返回参数说明：是否使用历元日期筛选器，true-使用

举    例：
```cpp
bool bUse = pIAdvCATAdvance->GetUseDateFilter();
```

### SetOutOfDate()

函数说明：设置历元日期阈值

输入类型：`double`

输入说明：历元日期阈值

举    例：
```cpp
pIAdvCATAdvance->SetOutOfDate(50000);
```

### GetOutOfDate()

函数说明：获取历元日期阈值

返回参数类型：`double`

返回参数说明：历元日期阈值

举    例：
```cpp
double dValue = pIAdvCATAdvance->GetOutOfDate();
```

### SetUsePeriApoFilter()

函数说明：设置是否使用远、近地点筛选器

输入类型：`bool`

输入说明：是否使用远、近地点筛选器

举    例：
```cpp
pIAdvCATAdvance->SetUsePeriApoFilter(true);
```

### GetUsePeriApoFilter()

函数说明：获取是否使用远、近地点筛选器

返回参数类型：`bool`

返回参数说明：是否使用远、近地点筛选器，true-使用

举    例：
```cpp
bool bUse = pIAdvCATAdvance->GetUsePeriApoFilter();
```

### SetPeriApoDist()

函数说明：设置远、近地点缓冲距离

输入类型：`double`

输入说明：远、近地点缓冲距离

举    例：
```cpp
pIAdvCATAdvance->SetPeriApoDist(50000);
```

### GetPeriApoDist()

函数说明：获取远、近地点缓冲距离

返回参数类型：`double`

返回参数说明：远、近地点缓冲距离

举    例：
```cpp
double dValue = pIAdvCATAdvance->GetPeriApoDist();
```

### SetUsePathFilter()

函数说明：设置是否使用轨道路径筛选器

输入类型：`bool`

输入说明：是否使用轨道路径筛选器

举    例：
```cpp
pIAdvCATAdvance->SetUsePathFilter(true);
```

### GetUsePathFilter()

函数说明：获取是否使用轨道路径筛选器

返回参数类型：`bool`

返回参数说明：是否使用轨道路径筛选器，true-使用

举    例：
```cpp
bool bUse = pIAdvCATAdvance->GetUsePathFilter();
```

### SetOrbitPath()

函数说明：设置轨道路径缓冲距离

输入类型：`double`

输入说明：轨道路径缓冲距离

举    例：
```cpp
pIAdvCATAdvance->SetOrbitPath(50000);
```

### GetOrbitPath()

函数说明：获取轨道路径缓冲距离

返回参数类型：`double`

返回参数说明：轨道路径缓冲距离

举    例：
```cpp
double dValue = pIAdvCATAdvance->GetOrbitPath();
```

### SetUseTimeFilter()

函数说明：设置是否使用时间筛选器

输入类型：`bool`

输入说明：是否使用时间筛选器

举    例：
```cpp
pIAdvCATAdvance->SetUseTimeFilter(true);
```

### GetUseTimeFilter()

函数说明：获取是否使用时间筛选器

返回参数类型：`bool`

返回参数说明：是否使用时间筛选器，true-使用

举    例：
```cpp
bool bUse = pIAdvCATAdvance->GetUseTimeFilter();
```

### SetTimeDist()

函数说明：设置时间缓冲距离

输入类型：`double`

输入说明：时间缓冲距离

举    例：
```cpp
pIAdvCATAdvance->SetTimeDist(50000);
```

### GetTimeDist()

函数说明：获取时间缓冲距离

返回参数类型：`double`

返回参数说明：时间缓冲距离

举    例：
```cpp
double dValue = pIAdvCATAdvance->GetTimeDist();
```

### SetEllipScaleFactor()

函数说明：设置椭球比例因子

输入类型：`double`

输入说明：椭球比例因子

举    例：
```cpp
pIAdvCATAdvance->SetEllipScaleFactor(50000);
```

### GetEllipScaleFactor()

函数说明：获取椭球比例因子

返回参数类型：`double`

返回参数说明：椭球比例因子

举    例：
```cpp
double dValue = pIAdvCATAdvance->GetEllipScaleFactor();
```

### SetUseHBRFile()

函数说明：设置是否使用SSC目标半径文件

输入类型：`bool`

输入说明：是否使用SSC目标半径文件

举    例：
```cpp
pIAdvCATAdvance->SetUseHBRFile(true);
```

### GetUseHBRFile()

函数说明：获取是否使用SSC目标半径文件

返回参数类型：`bool`

返回参数说明：是否使用SSC目标半径文件

举    例：
```cpp
bool bUse = pIAdvCATAdvance->GetUseHBRFile();
```

### SetRadiusFileName()

函数说明：设置SSC目标半径文件名

输入类型：`std::string`

输入说明：SSC目标半径文件路径

举    例：
```cpp
pIAdvCATAdvance->SetUseHBRFile("C:\\Users\\tempest\\Documents\\STK\\SSC.rad");
```

### GetRadiusFileName()

函数说明：获取SSC目标半径文件名

返回参数类型：`std::string`

返回参数说明：SSC目标半径文件路径

举    例：
```cpp
std::string strRadiusFileName = pIAdvCATAdvance->GetRadiusFileName();
```

### SetMaxStepSize()

函数说明：设置最大采样步长

输入类型：`double`

输入说明：最大采样步长

举    例：
```cpp
pIAdvCATAdvance->SetMaxStepSize(60);
```

### GetMaxStepSize()

函数说明：获取最大采样步长

返回参数类型：`double`

返回参数说明：最大采样步长

举    例：
```cpp
double dValue = pIAdvCATAdvance->GetMaxStepSize();
```

### SetMinStepSize()

函数说明：设置最小采样步长

输入类型：`double`

输入说明：最小采样步长

举    例：
```cpp
pIAdvCATAdvance->SetMinStepSize(60);
```

### GetMinStepSize()

函数说明：获取最小采样步长

返回参数类型：`double`

返回参数说明：最小采样步长

举    例：
```cpp
double dValue = pIAdvCATAdvance->GetMinStepSize();
```

### SetConjTcaType()

函数说明：设置tca阈值的类型

输入类型：`EConjTcaType`

输入说明：tca阈值的类型

举    例：
```cpp
pIAdvCATAdvance->SetConjTcaType(eGlobalOnly);
```

### GetConjTcaType()

函数说明：获取tca阈值的类型

返回参数类型：`EConjTcaType`

返回参数说明：tca阈值的类型

举    例：
```cpp
EConjTcaType eConjTcaType = pIAdvCATAdvance->GetConjTcaType();
```


