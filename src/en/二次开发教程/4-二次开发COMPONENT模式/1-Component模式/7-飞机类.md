# 飞机类

## 类 IAircraft 
 
> [!info]
> 类 IAircraft ，继承自类 IAtkObject

属性类型： `IAircraft*`

举  例： 
```cpp
IAircraft* pIAircraft = (IAircraft*)pIScen->GetChildren()->New(eAircraft, "IAircraft1");
```
 
###  GetGreatArcVehicle()

函数说明：获得飞机轨道预报器

返回参数类型： `IGreatArcVehicle*`

返回参数说明：轨道预报器

举  例： 
```cpp
IGreatArcVehicle* pIGreatArcVehicle = pIAircraft->GetGreatArcVehicle();
```

### GetGraphics()

函数说明：获得二维属性

返回参数类型：`IAcGraphics*`

返回参数说明：二维属性指针

举    例：
```cpp
IAcGraphics* pIAcGraphics = pIAircraft->GetGraphics();
```
 
### GetVO()

函数说明：获得三维属性

返回参数类型：`IAcVO*`

返回参数说明：三维属性指针

举    例：
```cpp
IAcVO* pIAcVO = pIAircraft->GetVO();
```
 
## 类 IGreatArcVehicle

### IsRouteTypeSupported()

函数说明：判断路径点类型是否支持

输入类型：`EVePropagatorType`

输入说明：路径点类型

返回参数类型：`bool`

返回参数说明：路径点类型是否支持

举  例 ：
```cpp
bool bSupport = plAircraft->GetGreatArcVehicle()>IsRouteTypeSupported(routeType);
```

### SetRouteType()

函数说明：设置路径点类型

输入类型：`EVePropagatorType`

输入说明：路径点类型

举  例 ：
```cpp
plAircraft->GetGreatArcVehicle()>SetRouteType(routeType);
```

### GetRouteType()

函数说明：获得路径点类型

返回参数类型：`EVePropagatorType`

返回参数说明：路径点类型

举  例 ：
```cpp
EVePropagatorType eType = plAircraft->GetGreatArcVehicle()>GetRouteType();
```
  
### GetRouteSupportedTypes()

函数说明：获得支持的路径点类型

返回参数类型： `std:vector<EVePropagatorType>`

返回参数说明：支持的路径点类型

举  例 ：
```cpp
std::vector<EVePropagator Type> vecType = plAircraft->GetGreatArcVehicle()>GetRouteSupportedTypes();
``` 

### GetRoute()
 
函数说明：获得路线

返回参数类型： `IVePropagator*`

返回参数说明：路线

举  例 ：
```cpp
IVePropagator* pIVePropagator = plAircraft->GetGreatArcVehicle()->GetRoute();
```

## 类 IVePropagator

## 类 IVePropagatorGreatArc
  
> [!info]
> 类 IVePropagatorGreatArc, 继承自类IVePropagator

### GetWaypoints()

函数说明：获得路径点集合

返回参数类型： `IVeWaypointsCollection*`

返回参数说明：路径点集合

举  例 ：
```cpp
IVeWaypointsCollection* pIVeWaypointsCollection= plVePropagatorGreatArc->GetWaypoints();
```

## 类 IVeWaypointsCollection

###  Add()

函数说明： 复制 Cobj 路径集合的最后一个元素，添加到集合最后；再用 Cobj 路径集合的新增元素，构建 Iobj 路径集合的新增元素

输出类型： `IVeWaypointsElement*`

输出说明： 路径点

举  例： 
```cpp
IVeWaypointsElement* pIVeWaypointsElement = pIVePropagatorGreatArc->GetWaypoints()->Add();
```

###  Contains()

函数说明： 确定集合是否包含指定时间的路径点

输入类型： `double`

输入说明： 指定时间

输出类型： `bool`

输出说明： 是否包含指定时间的路径点

举  例： 
```cpp
bool isContains = pIVePropagatorGreatArc->GetWaypoints()->Contains(wpv[1]->Time);
```
 
###  IndexOf()

函数说明： 确定集合是否包含指定的路径点并返回现有路径的索引。索引< 0 表示在集合中不存在的方式点。

输入类型： `double`

输入说明： 指定时间

输出类型： `int`

输出说明： 指定时间在集合中的索引

举  例： 
```cpp
int index = pIVePropagatorGreatArc->GetWaypoints()->IndexOf(123456);
```
 
###  RemoveAll()

函数说明： 从集合中删除所有元素

举  例： 
```cpp
pIVePropagatorGreatArc->GetWaypoints()->RemoveAll();
```

###  RemoveAt()

函数说明： 使用指定索引从集合中删除元素

输入类型： `int`

输入说明： 删除索引

举  例： 
```cpp
pIVePropagatorGreatArc->GetWaypoints()->RemoveAt(1);
```
 
###  ToArray()

函数说明： 返回一个包含方法点的二维数组。每个子数组都表示一个选择 点。元素的顺序是时间、纬度、经度、高度、速度、加速度、旋转半径

输出类型： `std::vector<IVeWaypointsElement*>`

输出说明： 路径点指针 Vector

举  例： 
```cpp
std::vector<IVeWaypointsElement*> wpv = pIVePropagatorGreatArc->GetWaypoints()->ToArray()
```

###  GetCount()

函数说明：获得路径点计数

返回参数类型： `int`

返回参数说明： 路径点计数

举  例： 
```cpp
int index = pIVeWaypointsCollection->GetCount(); 
```
  
###  Item ()
 
函数说明： 给定索引，返回集合中的一个元素(扩展函数)

输入类型： `int`  

输入说明： 索引

输出类型： `IveWaypointsElement*`

输出说明： 路径点指针

举  例： 
```cpp
IVeWaypointsElement* pIVeWaypointsElement = pIVeWaypointsCollection->Item(0);
```
 
## 类 IVeWaypointsElement
 
###  GetAcceleration() 

函数说明：获得加速度

返回参数类型： `double`

返回参数说明： 加速度

举  例： 
```cpp
double acceleration = pIVeWaypointsElement->GetAcceleration(); 
```
  
###  SetAcceleration()
 
函数说明： 设置加速度(扩展函数)

输入类型： `double`  

输入说明： 加速度

举  例： 
```cpp
pIVeWaypointsElement->SetAcceleration(0)
```
  
###  GetAltitude()
 
函数说明：获得高度

返回参数类型： `double`

返回参数说明： 高度

举  例： 
```cpp
double altitude = pIVeWaypointsElement->GetAltitude(); 
```
  
###  SetAltitude()
 
函数说明： 设置高度(扩展函数)

输入类型： `double`  

输入说明： 高度

举  例： 
```cpp
pIVeWaypointsElement->SetAltitude(0)
``` 

###  GetLatitude() 
 
函数说明：获得纬度

返回参数类型： `double`

返回参数说明： 纬度

举  例： 
```cpp
double latitude = pIVeWaypointsElement->GetLatitude(); 
```
  
###  SetLatitude()
 
函数说明： 设置纬度(扩展函数)

输入类型： `double`  

输入说明： 纬度

举  例： 
```cpp
pIVeWaypointsElement->SetLatitude(0)
``` 

###  GetLongitude()
 
函数说明：获得经度

返回参数类型： `double`

返回参数说明： 经度

举  例： 
```cpp
double longitude = pIVeWaypointsElement->GetLongitude(); 
```
  
###  SetLongitude()
 
函数说明： 设置经度(扩展函数)

输入类型： `double`  

输入说明： 经度

举  例： 
```cpp
pIVeWaypointsElement->SetLongitude(0)
```
  
###  GetSpeed() 
 
函数说明：获得速度

返回参数类型： `double`

返回参数说明： 速度

举  例： 
```cpp
double speed = pIVeWaypointsElement->GetSpeed(); 
```
  
###  SetSpeed()
 
函数说明： 设置速度(扩展函数)

输入类型： `double`  

输入说明： 速度

举  例： 
```cpp
pIVeWaypointsElement->SetSpeed (0)
```
  
###  GetTime() 
 
函数说明：获得时间 以秒为单位，表示和想定开始时间的间隔
 
返回参数类型： `double`

返回参数说明： 时间

举  例： 
```cpp
double time = pIVeWaypointsElement->GetTime(); 
```
  
###  SetTime()
 
函数说明： 设置时间(扩展函数)

输入类型： `double`  

输入说明： 时间

举  例： 
```cpp
pIVeWaypointsElement->SetTime(0)
```
  
###  GetTurnRadius() 
 
函数说明：获得在当前路点和下一个点之间的弧的曲率

返回参数类型： `double`

返回参数说明： 在当前路点和下一个点之间的弧的曲率

举  例： 
```cpp
double turnRadius = pIVeWaypointsElement->GetTurnRadius(); 
```
  
###  SetTurnRadius()
 
函数说明： 设置曲率(扩展函数)

输入类型： `double`  

输入说明： 曲率

举  例： 
```cpp
pIVeWaypointsElement->SetTurnRadius(0)
``` 

## 飞机二维

### 类 IGreatArcGraphics

举    例：
```cpp
IGreatArcGraphics* pIGreatArcGraphics= IGreatArcGraphics2IAcGraphics (pIAircraft->GetGraphics());
```
  
说    明：pIAircraft为IAircraft类指针

#### IsAttributesTypeSupported()

函数说明：判断二维显示是否支持此类型

输入类型：`EVeGfxAttributes`

输入说明：二维显示类型

返回参数类型：`bool`

返回参数说明：true-支持此类型

举    例：
```cpp
bool bSupport = pIGreatArcGraphics->IsAttributesTypeSupported(eAttributesBasic);
```
  
#### SetAttributesType()

函数说明：设置二维显示类型

输入类型：`EVeGfxAttributes`

输入说明：二维显示类型

举    例：
```cpp
pIGreatArcGraphics->SetAttributesType(eAttributesBasic);
```
  
#### GetPassData()

函数说明：获得二维轨迹属性

返回参数类型：`IVeGfxOrbitPassData*`

返回参数说明：二维属性指针

举    例：
```cpp
IVeGfxOrbitPassData* pIVeGfxOrbitPassData = pIGreatArcGraphics->GetPassData();
```
  
#### GetAttributes()

函数说明：获得二维显示类型属性

返回参数类型：`IVeGfxAttributes*`

返回参数说明：二维显示类型指针

举    例：
```cpp
IVeGfxAttributes* pIVeGfxAttributes = pIGreatArcGraphics->GetAttributes();
```
 
#### GetAttributesSupportedTypes()

函数说明：获得二维显示支持类型

返回参数类型：`std::vector<std::string>`

返回参数说明：二维显示支持类型

举    例：
```cpp
std::vector<std::string> vecAttSuportType = pIGreatArcGraphics->GetAttributesSupportedTypes();
```
 
#### GetAttributesType()

函数说明：获得二维显示类型

返回参数类型：`EVeGfxAttributes`

返回参数说明：二维显示类型

举    例：
```cpp
EVeGfxAttributes eAttr = pIGreatArcGraphics->GetAttributesType();
```
 
### 类 IAcGraphics
 
> [!info]
> 类 IAcGraphics，继承自类IGreatArcGraphics

举    例：IAcGraphics* pIAcGraphics = pIAircraft->GetGraphics();

说    明：pIAircraft为IAircraft类指针

## 飞机三维

### 类IAcVO

举    例：IAcVO* pIAcVO = pIAircraft->GetVO();

说    明：pIAircraft为IAircraft类指针

#### GetOffsets()

函数说明：获得三维模型属性

返回参数类型：`IVOOffset*`

返回参数说明：三维模型属性指针

举    例：
```cpp
IVOOffset* pIVOOffset = pIAcVO->GetOffsets();
```
 
#### GetVector()

函数说明：获得三维向量属性

返回参数类型：`IVOVector*`

返回参数说明：获得三维向量属性

举    例：
```cpp
IVOVector* pIVOVector = pIAcVO->GetVector();
```