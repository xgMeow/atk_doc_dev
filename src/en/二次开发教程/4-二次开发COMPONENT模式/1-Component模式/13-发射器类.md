# 发射器类


## 类ITransmitter

属性类型：发射器

举  例： 
```cpp
ITransmiter*pITransmitter=(ITransmitter*)plFacility->GetChildren()->New(eTransmitter,"ITransmitter");
```
 
### SetModel()

函数说明：按名称设置当前发送模型

输入类型： `std:string`

输入说明：发送模型名称(简单模型：`"Simple Transmitter Model"`)

举  例： 
```cpp
plTransmitter->SetModel("Simple Transmitter Model ");
```
 
### GetModel() 
 
函数说明：获得发送模型

返回参数类型： `ITransmitterModel*`

返回参数说明：发送模型

举  例 ：
```cpp
ITransmitterModel*plTransmitterModel = pITransmitter->GetModel();
```
 
## 类 ITransmitterModel

### GetName()

函数说明：获得模型名称

返回参数类型： `std:string`

返回参数说明：模型名称

举  例 ：
```cpp
plTransmitterModel>GetName();
```
 
### GetType()

函数说明：获得发送模型类型

返回参数类型： `ETransmitterModelType`

返回参数说明：接收模型类型

举  例 ：
```cpp
plTransmitterModel>GetType();
```
 
## 类 ITransmitterModelSimple 
 
> [!info]
> 类 ITransmitterModelSimple,  继承自类ITransmitterModel

属性类型： `ITransmitterModelSimple*`

属性说明：模型子类：简单

举  例 ：
```cpp
pITransmitter->SetModel("Simple  Transmitter  Model");

ITransmitterModelSimple*plTransmitterModelSimple    =(ITransmitterModelSimple*)pITransmitter->GetModel();
```
  
### GetFrequency()

函数说明：获得频率(GHz)

返回参数类型： `double`

返回参数说明： 频率(GHz)

举  例： 
```cpp
pITransmitterModelSimple->GetFrequency(); 
```
  
###  SetFrequency()

函数说明： 设置频率(扩展函数)

输入类型： `double`

输入说明： 频率

举  例： 
```cpp
pITransmitterModelSimple->SetFrequency(16.5);
```
  
###  GetDataRate() 
 
函数说明：获得数据速率(Mb/sec)

返回参数类型： `double`

返回参数说明： 数据速率(Mb/sec)

举  例： 
```cpp
pITransmitterModelSimple->GetDataRate(); 
```
  
###  SetDataRate()
 
函数说明： 设置数据速率(扩展函数)

输入类型： `double`

输入说明： 数据速率(Mb/sec)

举  例： 
```cpp
pITransmitterModelSimple->SetDataRate(17);
```
  
###  GetEirp() 
 
函数说明：获得 EIRP(dbW)

返回参数类型： `double`

返回参数说明： EIRP(dbW)

举  例： 
```cpp
pITransmitterModelSimple->GetEirp(); 
```
  
###  SetEirp()
 
函数说明： 设置 EIRP(扩展函数)
 
输入类型： `double`

输入说明： EIRP(dbW)

举  例： 
```cpp
pITransmitterModelSimple->SetEirp(18);
```
  
###  SetPolarizationType()
 
函数说明： 设置 天线极化类型 (子类之间类型转换，子类成员值不变)

输入类型： `EPolarizationType`

输入说明： 天线极化类型

举  例： 
```cpp
pITransmitterModelSimple->SetPolarizationType();
```
  
###  GetPolarization() 
 
函数说明：获得天线极化对象指针

返回参数类型： `IPolarization*`

返回参数说明： 天线极化

举  例： 
```cpp
IPolarization* pIPolarization = pITransmitterModelSimple->GetPolarization(); 
```
  
###  SetModulator()

函数说明： 按名称设置当前调制器模型

输入类型： `std::string`

输入说明： 调制器模型名称

举  例： 
```cpp
pITransmitterModelSimple->SetModulator();
```
  
###  GetModulator() 
 
函数说明：获得调制器

返回参数类型： `IModulatorModel*`

返回参数说明： 调制器

举  例： 
```cpp
IModulatorModel * pIModulatorModel = pITransmitterModelSimple->GetModulator(); 
```
  
## 类 IModulatorModel
 
###  GetName() 
 
函数说明：获得模型名称

返回参数类型： `std::string`

返回参数说明： 模型名称

举  例： 
```cpp
pIModulatorModel->GetName(); 
```
  
###  GetType() 
 
函数说明：获得模型类型

返回参数类型： `EDemodulatorModelType`

返回参数说明： 模型类型

举  例： 
```cpp
pIModulatorModel->GetType(); 
``` 

###  GetBandwidth() 
 
函数说明：获得带宽(MHz)

返回参数类型： `double`

返回参数说明： 带宽

举  例： 
```cpp
pIModulatorModel->GetBandwidth(); 
```
  
###  SetBandwidth()
 
函数说明： 设置带宽(扩展函数)

输入类型： `double`

输入说明： 带宽

举  例： 
```cpp
pITransmitterModelSimple->SetBandwidth(1);
```
  
##  类 IModulatorModelBpsk ，继承自类 IModulatorModel 

> [!note]
> 待补充


## 类 IModulatorModelQpsk ，继承自类 IModulatorModel

> [!note]
> 待补充

