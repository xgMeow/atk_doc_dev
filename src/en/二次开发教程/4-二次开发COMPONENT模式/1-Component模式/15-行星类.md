# 行星类

## 类 IPlanet
 
> [!info]
> 类 IPlanet, 继承自类IAtkObject

属性类型：行星

举  例： 
```cpp
IPlanet*pIPlanet  = (IPlanet*)pIScen->GetChildren()->New(ePlanet,"IPlanet");
```
  
### GetPositionSource() 
 
函数说明：获得位置资源类型

返回参数类型：`EPIPositionSourceType`

返回参数说明：位置资源类型

举  例： 
```cpp
EPIPositionSourceType type = plPlanet>GetPositionSource(); 
```
 
### SetPositionSource()
 
函数说明：设置位置资源类型

输入类型：`EPIPositionSourceType`

输入说明：位置资源类型

举  例 ：
```cpp
plPlanet->SetPositionSource(ePosCentralBody);
```
  
### GetPositionSourceData() 
 
函数说明：获得位置资源数据

返回参数类型： `IPositionSourceData*`

返回参数说明： 位置资源数据

举  例： 
```cpp
IPositionSourceData* pIPositionSourceData = pIPlanet->GetPositionSourceData();
``` 

### GetCommonTasks() 
 
函数说明：获得常见任务

返回参数类型： `IPlCommonTasks*`

返回参数说明： 常见任务

举  例： 
```cpp
IPlCommonTasks* pIPlCommonTasks = pIPlanet->GetCommonTasks(); 
```
  
## 类 IPositionSourceData
 
## 类 IPlPosCentralBody 
  
> [!info]
> 类 IPlPosCentralBody ，继承自类 IPositionSourceData

属性类型： `IPlPosCentralBody*`

属性说明： 资源位置子类：中心体

举  例： 
```cpp
IPositionSourceData* pIPositionSourceData = pIPlanet->GetPositionSourceData();

IPlPosCentralBody* pIPlPosCentralBody = (IPlPosCentralBody*)pIPositionSourceData;
```
  
### GetAutoRename() 
 
函数说明：获得是否自动重命名

返回参数类型： `bool`

返回参数说明： 是否自动重命名

举  例：
```cpp
bool isAutoRename = pIPlPosCentralBody->GetAutoRename(); 
```
  
### SetAutoRename()
 
函数说明： 设置是否自动重命名

输入类型： `bool`

输入说明： 是否自动重命名
 
举  例 ：
```cpp
pIPIPosCentralBody->SetAutoRename(true);
```
  
### GetCentralBody() 
  
函数说明：获得中央天体名称

返回参数类型： `std:string`

返回参数说明：中央天体名称

举  例 ：
```cpp
std:string strCentralBodyName = pIPIPosCentralBody->GetCentralBody(); 
```
  
### SetCentralBody()
 
函数说明：设置中央天体名称

输入类型： `std:string`

输入说明：中央天体名称

举  例 ：
```cpp
pIPIPosCentralBody->SetCentralBody(strCentralBodyName);
```
  
## 类 IPlCommonTasks
 
### SetPositionSourceCentralBody()

函数说明：使用中心体指定地球,并指定所需的中心体

输入类型： `std:string`

输入说明：中心体指针

举  例 ：
```cpp
pIPlanet>SetPositionSourceCentralBody(strCentralBodyName);
```

