# 链类

## 类 IChain 
 
> [!info]
> 类 IChain, 继承自类IAtkObject

属性类型：链

举  例： 
```cpp
IChain*plChain=(IChain*)plScen->GetChildren()->New(eChain, "IChain");
```

### GetObjects() 
 
函数说明：获得链中的对象集合
 
返回参数类型： `IObjectLinkCollection*`

返回参数说明： 链中的对象集合

举  例： 
```cpp
IObjectLinkCollection* pIObjectLinkCollection = pIChain->GetObjects(); 
```
 
### GetGraphics()

函数说明：获得二维属性

返回参数类型：`IChGraphics*`

返回参数说明：二维属性指针

举    例：
```cpp
IChGraphics* pIChGraphics = pIChain->GetGraphics(); 
```
 
## 类 IObjectLinkCollection
 
### Add()
  
函数说明： 增加链接对象通过对象路径名称

输入类型： `std::string`

输入说明： 对象路径名称

举  例： 
```cpp
pIObjectLinkCollection->Add(strObjPathName);
```
  
### AddObject()
 
函数说明： 增加链接对象通过对象指针

输入类型： `IAtkObject*`

输入说明： 对象指针

举  例： 
```cpp
pIObjectLinkCollection->AddObject(pIAtkObject);
```
  
### Contains()
 
函数说明： 搜索链接对象路径名称是否存在

输入类型： `std::string`

输入说明： 对象路径名称

举  例： 
```cpp
pIObjectLinkCollection->Contains(strObjPathName);
```
  
### IndexOf()
 
函数说明： 获得链接对象索引

输入类型： `std::string`

输入说明： 对象路径名称

举  例： 
```cpp
pIObjectLinkCollection->IndexOf(strObjPathName);
```
  
### Remove()
 
函数说明： 通过索引移除链接对象

输入类型： `int`

输入说明： 对象索引

举  例： 
```cpp
pIObjectLinkCollection->Remove(index);
```
  
### RemoveAll()
 
函数说明： 移除所有链接对象

举  例： 
```cpp
pIObjectLinkCollection->RemoveAll();
```
  
### RemoveName()
 
函数说明： 通过对象路径名称移除链接对象

输入类型： `std::string`

输入说明： 对象路径名称

举  例： 
```cpp
pIObjectLinkCollection->RemoveName(strObjPathName);
```
  
### RemoveObject()
 
函数说明： 通过对象指针移除链接对象

输入类型： `IAtkObject*`

输入说明： 对象指针

举  例： 
```cpp
pIObjectLinkCollection->RemoveObject(pIAtkObject);
```
  
### AvailableObjects()
 
函数说明： 获取所有链接对象

举  例： 
```cpp
pIObjectLinkCollection->AvailableObjects();
```
  
### GetCount()
 
函数说明： 获得链接对象数量

返回参数类型： `int`

返回参数说明： 链接对象数量

举  例： 
```cpp
int dValue = pIObjectLinkCollection->GetCount();
```
  
### Item()
 
函数说明： 通过索引获得链接对象

输入类型： `int`

输入说明： 对象索引
  
举  例： 
```cpp
IObjectLink* pIObjectLink = pIObjectLinkCollection->Item(index);
```
  
## 类 IObjectLink
 
### GetLinkedObject() 
 
函数说明：获得链接对象

返回参数类型： `IAtkObject*`

返回参数说明： 链接对象

举  例： 
```cpp
IAtkObject* pIAtkObject = pIObjectLink->GetLinkedObject(); 
```
  
### GetName() 
  
函数说明：获得名称

返回参数类型： `std::string`

返回参数说明： 名称

举  例： 
```cpp
string sName = pIObjectLink->GetName(); 
```

### GetPath() 
 
函数说明：获得路径名称

返回参数类型： `std::string`

返回参数说明： 路径名称

举  例： 
```cpp
string sPath = pIObjectLink->GetLinkedObject(); 
```
  
### GetType() 
 
函数说明：获得对象类型

返回参数类型： `EATKObjectType`

返回参数说明： 对象类型

举  例： 
```cpp
EATKObjectType eATKObjectType = pIObjectLink->GetType(); 
```
 
## 链二维

### 类IChGraphics

举    例：
```cpp
IChGraphics* pIChGraphics = pIChain->GetGraphics();
```
 
说    明：pIChain为IChain类指针

#### GetIsObjectGraphicsVisible()

函数说明：获得对象是否在二维显示

返回参数类型：`bool`

返回参数说明：是否显示

举    例：
```cpp
bool bIsShow = pIChGraphics->GetIsObjectGraphicsVisible();
```
 
#### SetIsObjectGraphicsVisible()

函数说明：设置对象是否在二维显示

输入类型：`bool`

输入说明：是否显示

举    例：
```cpp
pIChGraphics->SetIsObjectGraphicsVisible(bIsShow);
```
 
#### GetColor()

函数说明：获得二维显示颜色

返回参数类型：`int`

返回参数说明：二维显示颜色

举    例：
```cpp
int nColor = pIChGraphics->GetColor();
```

#### SetColor()

函数说明：设置二维显示颜色

输入类型：`int`

输入说明：二维显示颜色

举    例：
```cpp
pIChGraphics->SetColor(nColor);
```

#### GetStyle()

函数说明：获得线型

返回参数类型：`ELineStyle`

返回参数说明：线型

举    例：
```cpp
ElineStyle eLineStyle = pIChGraphics->GetStyle();
```

#### SetStyle()

函数说明：设置线型

输入类型：`ELineStyle`

输入说明：线型

举    例：
```cpp
pIChGraphics->SetStyle(eLineStyle);
```

#### GetLineWidth()

函数说明：获得线宽

返回参数类型：`ELineWidth`

返回参数说明：线宽

举    例：
```cpp
ElineWidth eLineWidth = pIChGraphics->GetLineWidth();
```

#### SetLineWidth()

函数说明：设置线宽

输入类型：`ELineWidth`

输入说明：线宽

举    例：
```cpp
pIChGraphics->SetLineWidth(eLineWidth);
```
