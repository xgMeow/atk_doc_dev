# 工具类

## 接近分析工具

### 类ICat

属性类型：接近分析工具

举    例：
```cpp
ICat* pICat = pIAtkObjectRoot->GetCat();
```

#### SetSatelliteFromScenario()

函数说明：根据卫星与TLE数据库对象计算

输入类型：`std::vector<std::string>`，`std::string`，`std::vector<std::string>`

输入说明：卫星对象名称，TLE数据文件路径，排除SSC编号

举    例：
```cpp
pICat->SetSatelliteFromScenario(vecSatelliteName，strTLEFilePath，vecExcSSCNum);
```

#### SetMissOrLauVehFromScenario()

函数说明：根据导弹/火箭与TLE数据库对象计算

输入类型：`std::vector<std::string>`，`std::string`，`std::vector<std::string>`

输入说明：导弹/火箭对象名称，TLE数据文件路径，排除SSC编号

举    例：
```cpp
pICat->SetMissOrLauVehFromScenario(vecObjName，strTLEFilePath，vecExcSSCNum);
```

#### SetInputSSCNumObject()

函数说明：输入目标国际编号

输入类型：`std::vector<std::string>`，`std::string`，`std::vector<std::string>`

输入说明：库文件SSC编号，TLE数据文件路径，排除SSC编号

举    例：
```cpp
pICat->SetInputSSCNumObject(vecSSCNumber，strTLEFilePath，vecExcSSCNum);
```

#### SetAllTLEObjects()

函数说明： 数据库目标遍历计算

输入类型：`std::string`，`std::vector<std::string>`

输入说明：库文件路径，排除SSC编号

举    例：
```cpp
pICat->SetAllTLEObjects(strTLEFilePath，vecExcSSCNum);
```

#### SetUseScenarioTimePeriod()

函数说明：是否使用场景时间

输入类型：`bool`

输入说明：是否使用场景区间

举    例：
```cpp
pICat->SetUseScenarioTimePeriod(true);
```

#### GetUseScenarioTimePeriod()

函数说明：获取是否使用场景时间

输出类型：`bool`

输出说明：获取是否使用场景时间

举    例：
```cpp
bool bUse = pICat->GetUseScenarioTimePeriod();
```

#### SetTimePeriod()

函数说明：设置仿真分析时间

输入类型：`std::string`，`std::string`

输入说明：仿真开始历元，仿真结束历元

举    例：
```cpp
pICat->SetTimePeriod(strStartTime，strEndTime);
```

#### GetTimePeriod()

函数说明：获取仿真分析时间

输出类型：`std::string`，`std::string`

输出说明：仿真开始历元，仿真结束历元

举    例：
```cpp
pICat->GetTimePeriod(strStartTime，strEndTime);
```

#### SetMaxRange()

函数说明：设置相对距离门限

输入类型：`double`

输入说明：相对距离门限

举    例：
```cpp
pICat->SetMaxRange(10000);
```

#### GetMaxRange()

函数说明：获取相对距离门限

输出类型：`double`

输出说明：相对距离门限

举    例：
```cpp
double dValue = pICat->GetMaxRange();
```

#### Compute()

函数说明：接近分析计算

举    例：
```cpp
pICat->Compute();
```

#### GetResults()

函数说明：获取计算结果

输出类型：`std::vector<std::string>`，`std::vector<std::string>`，`std::vector<double>`，`std::vector<double>`，`std::vector<double>`，`std::vector<double>`，`std::vector<double>`，`std::vector<double>`，`std::vector<double>``std::vector<std::string>`，`std::vector<std::string>`，`std::vector<std::string>`

输出说明：主目标SSC，次目标SSC，等效距离，相对距离，径向距离，横向距离，法向距离，接近夹角，相对速度，接近时刻，开始时刻，结束时刻

举    例：
```cpp
pICat->GetResults(PrimarySSC,SecondarySSC,EquivDist,MissDistance,MissDistR,MissDistT,MissDistN,AngleCA,RelativeVelocity,TCA,Tin,Tout);
```

#### Compute()

函数说明：接近分析计算

举    例：
```cpp
pICat->Compute();
```

#### CatComponent()

函数说明：接近分析计算

输出类型：`std::vector<double>`，`std::vector<std::vector<double>>`，`EVePropagatorType`，`std::string`，`std::string`，`std::string`，`std::string`，`double`，`double`，`std::vector<AsTCatRes>&`

输出说明：主目标卫星轨道根数，次目标卫星轨道根数，轨道预报模型与卫星对象对应，场景开始时间，场景结束时间，分析时段开始时间UTC，分析时段结束时间UTC，相对距离阈值，对速度阈值，结果

举    例：
```cpp
pICat->CatComponent(primElem,secElems,OrbPropType,startUTC,stopUTC,t1,t2,range,vel,result);
```

#### GetAdvance()

函数说明：高级配置指针

举    例：
```cpp
ICATAdvance* pICATAdvance* = pICat->GetAdvance();
```

### 类ICATAdvance

#### SetMethod()

函数说明：设置过滤器方法

输入类型：`ECATMethod`

输入说明：过滤器方法

举    例：
```cpp
pICat->ICATAdvance(eAnalyticalMethod);
```

#### GetMethod()

函数说明：获取过滤器方法

输出类型：`ECATMethod`

输出说明：过滤器方法

举    例：
```cpp
ECATMethod eType = pICat->GetMethod();
```

#### SetUseDateFilter()

函数说明：设置是否使用历元日期筛选器

输入类型：`bool`

输入说明：是否使用历元日期筛选器

举    例：
```cpp
pICat->SetUseDateFilter(true);
```

#### GetUseDateFilter()

函数说明：获取是否使用历元日期筛选器

输出类型：`bool`

输出说明：是否使用历元日期筛选器

举    例：
```cpp
bool bUse = pICat->GetUseDateFilter();
```

#### SetOutOfDate()

函数说明：设置历元日期阈值

输入类型：`double`

输入说明：历元日期阈值

举    例：
```cpp
pICat->SetOutOfDate(2592000);
```

#### GetOutOfDate()

函数说明：获取历元日期阈值

输出类型：`double`

输出说明：历元日期阈值

举    例：
```cpp
double dValue = pICat->GetOutOfDate();
```

#### SetUsePeriApoFilter()

函数说明：设置是否使用远、近地点筛选器

输入类型：`bool`

输入说明：是否使用远、近地点筛选器

举    例：
```cpp
pICat->SetUsePeriApoFilter(true);
```

#### GetUsePeriApoFilter()

函数说明：获取是否使用远、近地点筛选器

输出类型：`bool`

输出说明：是否使用远、近地点筛选器

举    例：
```cpp
bool bUse = pICat->GetUsePeriApoFilter();
```

#### SetPeriApoDist()

函数说明：设置远、近地点缓冲距离

输入类型：`double`

输入说明：远、近地点缓冲距离

举    例：
```cpp
pICat->SetPeriApoDist(2592000);
```

#### GetPeriApoDist()

函数说明：获取远、近地点缓冲距离

输出类型：`double`

输出说明：远、近地点缓冲距离

举    例：
```cpp
double dValue = pICat->GetPeriApoDist();
```

#### SetUsePathFilter()

函数说明：设置是否使用轨道路径筛选器

输入类型：`bool`

输入说明：是否使用轨道路径筛选器

举    例：
```cpp
pICat->SetUsePathFilter(true);
```

#### GetUsePathFilter()

函数说明：获取是否使用轨道路径筛选器

输出类型：`bool`

输出说明：是否使用轨道路径筛选器

举    例：
```cpp
bool bUse = pICat->GetUsePathFilter();
```

#### SetOrbitPath()

函数说明：设置轨道路径缓冲距离

输入类型：`double`

输入说明：轨道路径缓冲距离

举    例：
```cpp
pICat->SetOrbitPath(100000);
```

#### GetOrbitPath()

函数说明：获取轨道路径缓冲距离

输出类型：`double`

输出说明：轨道路径缓冲距离

举    例：
```cpp
double dValue = pICat->GetOrbitPath();
```

#### SetUseTimeFilter()

函数说明：设置是否使用时间筛选器

输入类型：`bool`

输入说明：是否使用时间筛选器

举    例：
```cpp
pICat->SetUseTimeFilter(true);
```

#### GetUseTimeFilter()

函数说明：获取是否使用时间筛选器

输出类型：`bool`

输出说明：是否使用时间筛选器

举    例：
```cpp
bool bUse = pICat->GetUseTimeFilter();
```

#### SetTimeDist()

函数说明：设置时间缓冲距离

输入类型：`double`

输入说明：时间缓冲距离

举    例：
```cpp
pICat->SetTimeDist(100000);
```

#### GetTimeDist()

函数说明：获取时间缓冲距离

输出类型：`double`

输出说明：时间缓冲距离

举    例：
```cpp
double dValue = pICat->GetTimeDist();
```

#### SetSimulationStep()

函数说明：设置分析步长

输入类型：`double`

输入说明：分析步长

举    例：
```cpp
pICat->SetSimulationStep(100000);
```

#### GetSimulationStep()

函数说明：获取分析步长

输出类型：`double`

输出说明：分析步长

举    例：
```cpp
double dValue = pICat->GetSimulationStep();
```

#### SetYellowDistThreshold()

函数说明：设置黄色告警

输入类型：`double`

输入说明：黄色告警

举    例：
```cpp
pICat->SetYellowDistThreshold(200);
```

#### GetYellowDistThreshold()

函数说明：获取黄色告警

输出类型：`double`

输出说明：黄色告警

举    例：
```cpp
double dValue = pICat->GetYellowDistThreshold();
```

#### SetRedDistThreshold()

函数说明：设置红色告警

输入类型：`double`

输入说明：红色告警

举    例：
```cpp
pICat->SetRedDistThreshold(200);
```

#### GetRedDistThreshold()

函数说明：获取红色告警

输出类型：`double`

输出说明：红色告警

举    例：
```cpp
double dValue = pICat->GetRedDistThreshold();
```

#### SetEquivalFactor()

函数说明：设置等效距离系数

输入类型：`double`

输入说明：等效距离系数

举    例：
```cpp
pICat->SetEquivalFactor(200);
```

#### GetEquivalFactor()

函数说明：获取等效距离系数

输出类型：`double`

输出说明：等效距离系数

举    例：
```cpp
double dValue = pICat->GetEquivalFactor();
```


