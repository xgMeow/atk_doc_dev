# ExecuteCommand

## 作用

在当前的ATK引擎中执行Connect命令

## 语法

```atks
ExecuteCommand("command", "objPath", "cmdParamString")
```

## 输入参数

`command`        - 具体请查看Connect 命令库
`objPath`        - 接受命令的对象路径
`cmdParamString` - 命令属性字符串

## 返回参数

拥有返回值的命令，会直接输出返回值字符串。
