# Local View

By default, the Earth inertial frame is the main viewpoint. Click <kbd>Local View</kbd> to switch to a specified central body or configured object as the primary viewpoint during simulation, focusing on the state changes of the target object.