# Communications Constraints

## Function Description

Communications Constraints are used to analyze whether the communications link between a transmitter and a receiver satisfies the specified conditions. They apply to tasks such as satellite communications, inter-satellite links, ground station communications, telemetry and telecommand, and data transmission. The system evaluates whether the current communications link is usable according to link metrics such as frequency, received power, signal-to-noise ratio, and bit error rate: if the link metric value falls within the specified range, the access satisfies the Communications Constraints; otherwise, it is considered an invalid access.

All parameters support **Min**/**Max** values and can be used together with **Reverse Selection** (exclude accesses within the specified range).

## Parameter Descriptions

<Entry meta="Dimension: Frequency|Unit: GHz" name="Frequency" desc="The operating frequency range of the communications link." objects="Transmitter,Receiver">

Limits the range of frequencies to which the transmitter or receiver responds. You can select a particular band from the 【Band】 drop-down list, or choose 【Custom】 to specify the frequency range.

Select 【Min】 or 【Max】 and enter a value to specify the value range in which accesses are allowed; enable 【Reverse Selection】 to remove accesses that fall within that range and retain only those outside it.

For the calculation formula, see [Frequency](../../../04-理论基础/02-约束配置/04-通信约束.md#frequency).

</Entry>

<Entry meta="Dimension: Temperature|Unit: K" name="System Noise Temperature" desc="The noise temperature of the communications system." objects="Receiver">

Select 【Min】 or 【Max】 and enter a value to specify the value range in which accesses are allowed; enable 【Reverse Selection】 to remove accesses that fall within that range and retain only those outside it.

For the calculation formula, see [System Noise Temperature](../../../04-理论基础/02-约束配置/04-通信约束.md#system-noise-temperature).

</Entry>

<Entry meta="Dimension: Power|Unit: dBW" name="Rcvd Isotropic Power" desc="The power level that could be received if the receiving end used an ideal isotropic antenna." objects="Transmitter,Receiver">

Select 【Min】 or 【Max】 and enter a value to specify the value range in which accesses are allowed; enable 【Reverse Selection】 to remove accesses that fall within that range and retain only those outside it.

For the calculation formula, see [Rcvd Isotropic Power](../../../04-理论基础/02-约束配置/04-通信约束.md#rcvd-isotropic-power).

</Entry>

<Entry meta="Dimension: Power Density|Unit: dBW/m²" name="Flux Density" desc="The electromagnetic power density per unit area at the receiving end." objects="Transmitter,Receiver">

Select 【Min】 or 【Max】 and enter a value to specify the value range in which accesses are allowed; enable 【Reverse Selection】 to remove accesses that fall within that range and retain only those outside it.

For the calculation formula, see [Flux Density](../../../04-理论基础/02-约束配置/04-通信约束.md#flux-density).

</Entry>

<Entry meta="Dimension: Power|Unit: dBW" name="Link EIRP" desc="The equivalent isotropically radiated power computed in the direction of the link between the transmitter and the receiver." objects="Transmitter,Receiver">

Select 【Min】 or 【Max】 and enter a value to specify the value range in which accesses are allowed; enable 【Reverse Selection】 to remove accesses that fall within that range and retain only those outside it.

For the calculation formula, see [Link EIRP](../../../04-理论基础/02-约束配置/04-通信约束.md#link-eirp).

</Entry>

<Entry meta="Dimension: Frequency|Unit: GHz" name="Doppler Shift" desc="The frequency offset caused by the relative velocity of the objects involved." objects="Transmitter,Receiver">

Select 【Min】 or 【Max】 and enter a value to specify the value range in which accesses are allowed; enable 【Reverse Selection】 to remove accesses that fall within that range and retain only those outside it.

For the calculation formula, see [Doppler Shift](../../../04-理论基础/02-约束配置/04-通信约束.md#doppler-shift).

</Entry>

<Entry meta="Dimension: SNR Spectral Density|Unit: dB·Hz" name="C/No" desc="The ratio of carrier power to noise power spectral density measured at the receiving antenna output." objects="Transmitter,Receiver">

Select 【Min】 or 【Max】 and enter a value to specify the value range in which accesses are allowed; enable 【Reverse Selection】 to remove accesses that fall within that range and retain only those outside it.

For the calculation formula, see [C/N₀](../../../04-理论基础/02-约束配置/04-通信约束.md#c-no).

</Entry>

<Entry meta="Dimension: Power|Unit: dBW" name="Power at Receiver Input" desc="The received signal power at the input of the receiver front-end amplifier." objects="Transmitter,Receiver">

Select 【Min】 or 【Max】 and enter a value to specify the value range in which accesses are allowed; enable 【Reverse Selection】 to remove accesses that fall within that range and retain only those outside it.

For the calculation formula, see [Power at Receiver Input](../../../04-理论基础/02-约束配置/04-通信约束.md#receiver-input-power).

</Entry>

<Entry meta="Dimension: SNR|Unit: dB" name="C/N" desc="The ratio of true carrier power to noise power." objects="Transmitter,Receiver">

Select 【Min】 or 【Max】 and enter a value to specify the value range in which accesses are allowed; enable 【Reverse Selection】 to remove accesses that fall within that range and retain only those outside it.

For the calculation formula, see [C/N](../../../04-理论基础/02-约束配置/04-通信约束.md#c-n).

</Entry>

<Entry name="Bit Error Rate" desc="Bit error rate." objects="Transmitter,Receiver">

Select 【Min】 or 【Max】 and enter a value to specify the value range in which accesses are allowed; enable 【Reverse Selection】 to remove accesses that fall within that range and retain only those outside it.

For the calculation formula, see [Bit Error Rate](../../../04-理论基础/02-约束配置/04-通信约束.md#bit-error-rate).

</Entry>

<Entry name="Link Margin" desc="The margin of the actual link performance over the minimum communications requirement." objects="Transmitter,Receiver">

Select 【Min】 or 【Max】 and enter a value to specify the value range in which accesses are allowed; enable 【Reverse Selection】 to remove accesses that fall within that range and retain only those outside it.

For the calculation formula, see [Link Margin](../../../04-理论基础/02-约束配置/04-通信约束.md#link-margin).

</Entry>

<Entry meta="Dimension: SNR|Unit: dB" name="Eb/No" desc="The ratio of energy per bit to noise power spectral density." objects="Transmitter,Receiver">

Select 【Min】 or 【Max】 and enter a value to specify the value range in which accesses are allowed; enable 【Reverse Selection】 to remove accesses that fall within that range and retain only those outside it.

For the calculation formula, see [Eb/N₀](../../../04-理论基础/02-约束配置/04-通信约束.md#eb-no).

</Entry>

<Entry name="Polarization Relative Angle" desc="The relative angle between the polarization plane of the transmitting antenna and the polarization plane of the receiving antenna." objects="Transmitter,Receiver">

Select 【Min】 or 【Max】 and enter a value to specify the value range in which accesses are allowed; enable 【Reverse Selection】 to remove accesses that fall within that range and retain only those outside it.

For the calculation formula, see [Polarization Relative Angle](../../../04-理论基础/02-约束配置/04-通信约束.md#polarization-relative-angle).

</Entry>

<Entry meta="Dimension: Sensitivity|Unit: dB/K" name="G/T" desc="The ratio of the link budget receiving gain value to the total receiver noise temperature along the link direction." objects="Transmitter,Receiver">

Select 【Min】 or 【Max】 and enter a value to specify the value range in which accesses are allowed; enable 【Reverse Selection】 to remove accesses that fall within that range and retain only those outside it.

For the calculation formula, see [G/T](../../../04-理论基础/02-约束配置/04-通信约束.md#g-t).

</Entry>
