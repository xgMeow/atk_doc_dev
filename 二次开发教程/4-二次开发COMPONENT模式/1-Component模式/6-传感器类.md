# 传感器类 

属性类型： `ISensor *`

举  例： 
```cpp
ISensor* pISensor = (ISensor*)pIFacility->GetChildren()->New(eSensor, "Sensor1");
```

## 类 ISensor 
 
> [!info]
> 类 ISensor ，继承自类 IAtkObject

###  GetPatternType()
 
函数说明： 获得传感器类型

返回参数类型： `ESnPattern`

返回参数说明： 传感器类型

举  例： 
```cpp
EsnPattern type = pISensor->GetPatternType();
```
 
###  SetPatternType()

函数说明： 设置传感器类型

输入类型： `ESnPattern`

输入说明： 传感器类型

举  例： 
```cpp
pISensor->SetPatternType(eSnSimpleConic);
```

###  GetPattern()
 
函数说明：获得传感器

返回参数类型： `IsnPattern*`

返回参数说明： 传感器

举  例： 
```cpp
IsnPattern* pIsnPattern = pISensor->GetPattern(); 
```
  
###  GetLocationType()
 
函数说明：获得位置类型

返回参数类型： `ESnLocation`

返回参数说明： 位置类型

举  例： 
```cpp
EsnLocation type = pISensor->GetLocationType(); 
```
  
###  SetLocationType()
 
函数说明： 设置位置类型

输入类型： `ESnLocation`

输入说明： 传感器位置类型

举  例： 
```cpp
pISensor->SetLocationType(eSnFixed) ;
```
  
###  GetLocationData() 
 
函数说明：获得位置

返回参数类型： `ILocationData*`

返回参数说明： 位置

举  例： 
```cpp
ILocationData* pILocationData = pISensor->GetLocationData(); 
```

###  GetPointingType() 
 
函数说明：获得传感器指向类型

返回参数类型： `ESnPointing`

返回参数说明： 传感器指向类型

举  例： 
```cpp
EsnPointing type = pISensor->GetPointingType(); 
```
  
###  SetPointingType()
 
函数说明： 设置传感器指向类型

输入类型： `ESnPointing`

输入说明： 传感器指向类型

举  例： 
```cpp
pISensor->SetPointingType(eSnPtFixed);
```
  
###  GetPointing() 
 
函数说明：获得指向

返回参数类型： `ISnPointing*`

返回参数说明： 指向

举  例： 
```cpp
ISnPointing* pISnPointing = pISensor->GetPointing(); 
``` 

###  GetCommonTasks() 
 
函数说明：获得常见任务

返回参数类型： `ISnCommonTasks*`

返回参数说明： 常见任务

举  例： 
```cpp
ISnCommonTasks* pISnCommonTasks = pISensor->GetCommonTasks(); 
```
 
### GetGraphics()

函数说明： 获得二维属性

返回参数类型： `ISnGraphics*`

返回参数说明： 二维属性指针

举    例： 
```cpp
ISnGraphics* pISnGraphics = pISensor->GetGraphics();
```

### GetVO()

函数说明： 获得三维属性

返回参数类型： `ISnVO*`

返回参数说明： 三维属性指针

举    例： 
```cpp
ISnVO* pISnVO = pISensor->GetVO();
```

## 类 ISnPattern
 
## 类 ISnRectangularPattern

> [!info]
> 类 ISnRectangularPattern ，继承自类 IPattern

属性类型：传感器子类矩形

举  例： 
```cpp
ISnRectangularPattern* pISnRectangularPattern = pISensor->GetCommonTasks()->SetPatternRectangular(46, 47);
```
 
###  SetAngularResolution()
 
函数说明： 设置点之间的角分离

输入类型： `double`

输入说明： 点之间的角分离

举  例： 
```cpp
pISnRectangularPattern->SetAngularResolution(1.0); ？？？ZY
```

###  GetAngularResolution()

函数说明：获得点之间的角分离

返回参数类型： `double`

返回参数说明： 点之间的角分离

举  例： 
```cpp
double dAR = pISnRectangularPattern->GetAngularResolution(); 
```
  
###  SetHorizontalHalfAngle()
 
函数说明： 设置水平半推角

输入类型： `double`

输入说明： 水平半推角

举  例： 
```cpp
pISnRectangularPattern->SetHorizontalHalfAngle(1.0); ？？？ZY
```

###  GetHorizontalHalfAngle() 
 
函数说明：获得水平半推角

返回参数类型： `double`

返回参数说明： 水平半推角

举  例： 
```cpp
double dHHF = pISnRectangularPattern->GetHorizontalHalfAngle(); 
```

###  SetVerticalHalfAngle()
 
函数说明： 设置垂直半推角

输入类型： `double`

输入说明： 垂直半推角

举  例： 
```cpp
pISnRectangularPattern->SetVerticalHalfAngle(1.0); ？？？ZY
```

###  GetVerticalHalfAngle() 
 
函数说明：获得垂直半推角

返回参数类型： `double`

返回参数说明： 垂直半推角

举  例： 
```cpp
double dVHA = pISnRectangularPattern->GetVerticalHalfAngle(); 
```
  
## 类 ISnSimpleConicPattern

> [!info]
> 类 ISnSimpleConicPattern ，继承自类 IPattern

属性类型： `ISnSimpleConicPattern*`

举  例： 
```cpp
ISnSimpleConicPattern* pISnSimpleConicPattern = pISensor->GetCommonTasks()->SetPatternSimpleConic(40, 0.1);
```

###  SetAngularResolution()
 
函数说明： 设置点之间的角分离

输入类型： `double`

输入说明： 点之间的角分离

举  例： 
```cpp
plSnSimpleConicPattern->SetAngularResolution(1.0); ？？？ZY
```

###  GetAngularResolution() 
  
函数说明：获得点之间的角分离

返回参数类型： `double`

返回参数说明： 点之间的角分离
 
举  例 ：
```cpp
double dAR = plSnSimpleConicPattern->GetAngularResolution();
```

###  SetConeAngle()
 
函数说明： 设置半锥角

输入类型： `double`

输入说明： 半锥角

举  例： 
```cpp
plSnSimpleConicPattern->SetConeAngle(1.0); ？？？ZY
```

### GetConeAngle() 
 
函数说明： 获得半锥角

返回参数类型： `double`

返回参数说明： 半锥角

举  例 ：
```cpp
double dCA = plSnSimpleConicPattern->GetConeAngle(); 
```
 
## 类 ILocationData

## 类 IPosition, 继承自类 ILocationData
 
> [!info]
> 类 IPosition, 继承自类 ILocationData

属性类型：`IPosition*`

举  例 ：
```cpp
IPosition* pIPosition = plSensor>GetPosition();
```

## 类 ISnPointing
 
## 类 ISnPtFixed
 
> [!info]
> 类 ISnPtFixed, 继承自类IPointing

属性类型： `ISnPtFixed*`

举  例 ：
```cpp
plSensor>SetPointingType(eSnPtFixed);

ISnPtFixed*plSnPtFixed  =(ISnPtFixed*)plSensor->GetPointing();
```
 
### GetOrientation()

函数说明：获得定向

返回参数类型： `IOrientation*`

返回参数说明：定向

举  例 ：
```cpp
IOrientation* pIOrientation = plSnPtFixed->GetOrientation();
```
 
## 类 IOrientation

### Assign()

函数说明： 设置定向

输入类型： `IOrientation*`

输入说明： 定向信息

举  例： 
```cpp
pISnPtFixed->GetOrientation()->Assign(pIOrientation);
```
 
###  AssignEulerAngles()

函数说明： 设置欧拉角定向

输入类型： `EEulerOrientationSequence`, `double`, `double`, `double`

输入说明： 欧拉角序列, 欧拉角 a, 欧拉角 b, 欧拉角 c

举  例： 
```cpp
pISnPtFixed->GetOrientation()->AssignEulerAngles (e121, 11.31, 8.97, 11.31) ;
```
 
###  AssignQuaternion()

函数说明： 设置四元数定向

输入类型： `double`, `double`, `double`, `double`

输入说明： 四元数 x, 四元数 y, 四元数 z, 四元数 s

举  例： 
```cpp
pISnPtFixed->GetOrientation()->AssignQuaternion(0.2, 0.08, 0, 1) ;
```

###  ConvertTo()

函数说明： 转换坐标类型， new 一个子类新空间返回，自身值不改变

输入类型： `EOrientationType`

输入说明： 转换类型

输出类型： `Iorientation*`

输出说明： 转换后类型指针

举  例： 
```cpp
pISnPtFixed->GetOrientation()->ConvertTo(eQuaternion);
```
 
###  QueryEulerAngles()

函数说明： 获得辅助定向方法使用欧拉角

输入类型： `EEulerOrientationSequence` `double`, `double` , `double`

输入说明： 欧拉角序列, 欧拉角 a, 欧拉角 b, 欧拉角 c

举  例： 
```cpp
pIFacility->GetPosition()->QueryEulerAngles(e121, a, b, c);
```
 
###  QueryQuaternion()

函数说明： 获得辅助定向方法使用四元数

输入类型： `double`, `double`, `double`, `double`

输入说明： 四元数 x, 四元数 y, 四元数 z, 四元数 s

举  例： 
```cpp
pIFacility->GetPosition()->QueryQuaternion(x, y, z, s);
```
 
###  GetOrientationType()

函数说明：获得定向类型

返回参数类型： `EOrientationType`

返回参数说明： 定向类型

举  例： 
```cpp
EorientationType type = pISensor->GetOrientationType();
```
 
## 类 IOrientationEulerAngles
  
> [!info]
> 类 IOrientationEulerAngles，继承自类IOrientation

属性类型： `IOrientationEulerAngles*`

举  例： 
```cpp
IOrientation* pIOrientation = pISnPtFixed->GetOrientation();

IOrientationEulerAngles* pIOrientationEulerAngles = (IOrientationEulerAngles*)pIOrientation;
```

###  SetA()

函数说明：设置欧拉角 A

输入类型： `double`

输入说明： 欧拉角 A

举  例： 
```cpp
pIOrientationEulerAngles->SetA(1.0);
```

###  SetB()

函数说明：设置欧拉角 B

输入类型： `double`

输入说明： 欧拉角 B

举  例： 
```cpp
pIOrientationEulerAngles->SetB(1.0);
```
 
###  SetC()

函数说明：设置欧拉角 C

输入类型： `double`

输入说明： 欧拉角 C

举  例： 
```cpp
pIOrientationEulerAngles->SetC(1.0);
```
 
###  SetSequence()

函数说明：设置欧拉角序列

输入类型： `EEulerOrientationSequence`

输入说明： 欧拉角序列

举  例： 
```cpp
pIOrientationEulerAngles->SetSequence(e121);
```

###  GetA()

函数说明：获得欧拉角 A

返回参数类型： `double`

返回参数说明： 欧拉角 A

举  例： 
```cpp
double dA = pIOrientationEulerAngles->GetA();
```

###  GetB()

函数说明：获得欧拉角 B

返回参数类型： `double`

返回参数说明： 欧拉角 B

举  例： 
```cpp
double dB = pIOrientationEulerAngles->GetB();
```
 
###  GetC()

函数说明：获得欧拉角 C

返回参数类型： `double`

返回参数说明： 欧拉角 C

举  例： 
```cpp
double dC = pIOrientationEulerAngles->GetC();
```
 
###  GetSequence()

函数说明：获得欧拉角序列

返回参数类型： `EEulerOrientationSequence`

返回参数说明： 欧拉角序列

举  例： 
```cpp
EEulerOrientationSequence dSeq = pIOrientationEulerAngles->GetSequence();
```
 
## 类 IOrientationQuaternion，继承自类 IOrientation

> [!info]
> 类 IOrientationQuaternion，继承自类 IOrientation

属性类型： `IOrientationQuaternion*`

举  例： 
```cpp
IOrientation* pIOrientation = pISnPtFixed->GetOrientation();

IOrientationQuaternion* pIOrientationQuaternion = (IOrientationQuaternion*)pIOrientation;
```

###  SetQX()

函数说明：设置四元数 x

输入类型： `double`

输入说明： 四元数 x

举  例： 
```cpp
pIOrientationQuaternion->SetQX(1.0);
``` 

###  SetQY()

函数说明：设置四元数 y

输入类型： `double`

输入说明： 四元数 y

举  例： 
```cpp
pIOrientationQuaternion->SetQY(1.0);
```
 
###  SetQZ()
 
函数说明：设置四元数 z

输入类型： `double`

输入说明： 四元数 z

举  例： 
```cpp
pIOrientationQuaternion->SetQZ(1.0);
```
 
###  SetQS()

函数说明：设置四元数 s

输入类型： `double`

输入说明： 四元数 s

举  例： 
```cpp
pIOrientationQuaternion->SetQS(1.0);
```

###  GetQX()

函数说明：获得四元数 x

返回参数类型： `double`

返回参数说明： 四元数 x

举  例： 
```cpp
double dQX = pIOrientationQuaternion->GetQX();
``` 

###  GetQY()

函数说明：获得四元数 y

返回参数类型： `double`

返回参数说明： 四元数 y

举  例： 
```cpp
double dQY = pIOrientationQuaternion->GetQY();
```
 
###  GetQZ()
 
函数说明：获得四元数 z

返回参数类型： `double`

返回参数说明： 四元数 z

举  例： 
```cpp
double dQZ = pIOrientationQuaternion->GetQZ();
```
 
###  GetQS()

函数说明：获得四元数 s

返回参数类型： `double`

返回参数说明： 四元数 s

举  例： 
```cpp
double dQS = pIOrientationQuaternion->GetQS();
```
 
## 类ISnCommonTasks

###  SetPatternSimpleConic()

函数说明： 设置圆锥传感器，返回指针，改变返回值，源数据也会跟着改变

输入类型： `double`, `double`

输入说明： 半锥角, 点之间的角分离

输出类型： `IsnSimpleConicPattern*`

输出说明： 圆锥传感器类型指针

举  例： 
```cpp
ISnSimpleConicPattern* pISnSimpleConicPattern = pISensor->GetCommonTasks()->SetPatternSimpleConic(40, 0.1);
```
 
###  SetPatternRectangular()

函数说明： 设置矩形传感器，返回指针，改变返回值，源数据也会跟着改 变

输入类型： `double`, `double`

输入说明： 垂直半推角, 水平半推角

输出类型： `IsnRectangularPattern*`

输出说明： 矩形传感器类型指针

举  例： 
```cpp
ISnRectangularPattern* pISnRectangularPattern = pISensor->GetCommonTasks()->SetPatternRectangular(46, 47);
```

###  SetPointingFixedEuler()

函数说明： 设置固定欧拉值角度，返回指针，改变返回值，源数据也会跟 着改变

输入类型： `EEulerOrientationSequence`, `double`, `double`, `double`

输入说明： 欧拉角序列, 欧拉角 a, 欧拉角 b, 欧拉角 c

输出类型： `IsnPtFixed*`

输出说明： 传感器固定指向指针

举  例： 
```cpp
ISnPtFixed* pISnPtFixede = pISensor->GetCommonTasks()->SetPointingFixedEuler(e121, 11.31, 8.97, 11.31);
```
 
###  SetPointingFixedQuat()

函数说明： 设置固定四元数角度，返回指针，改变返回值，源数据也会跟 着改变

输入类型： `double`, `double`, `double`, `double`

输入说明： 四元数 x, 四元数 y, 四元数 z, 四元数 s

输出类型： `IsnPtFixed*`

输出说明： 传感器固定指向指针

举  例： 
```cpp
ISnPtFixed* pISnPtFixedq = pISensor->GetCommonTasks()->SetPointingFixedQuat(0.2, 0.08, 0, 1);
```

## 传感器二维

### 类ISnGraphics

举    例：ISnGraphics* pISnGraphics = pIFacility->GetGraphics();

说    明：pISensor为ISensor类指针

#### GetIsObjectGraphicsVisible()

函数说明： 获得对象是否在二维显示

返回参数类型： `bool`

返回参数说明： 是否显示

举    例：
```cpp
bool bIsShow = pISnGraphics->GetIsObjectGraphicsVisible();
```

#### SetIsObjectGraphicsVisible()

函数说明： 设置对象是否在二维显示

输入类型： `bool`

输入说明： 是否显示

举    例：
```cpp
pISnGraphics->SetIsObjectGraphicsVisible(bIsShow); 
```

#### GetFillVisible()

函数说明： 获得对象是否填充

返回参数类型： `bool`

返回参数说明： 是否填充

举    例：
```cpp
bool bIsShow = pISnGraphics->GetFillVisible();
```

#### SetFillVisible()

函数说明： 设置对象是否填充

输入类型： `bool`

输入说明： 是否填充

举    例： 
```cpp
pISnGraphics->SetFillVisible(bIsShow); 
```

#### GetColor()

函数说明： 获得二维显示颜色

返回参数类型： `int`

返回参数说明： 二维显示颜色

举    例：
```cpp
int nColor = pISnGraphics->GetColor();
```

#### SetFillVisible()

函数说明： 设置对象是否填充

输入类型： `bool`

输入说明： 是否填充

举    例：
```cpp
pISnGraphics->SetFillVisible(bIsShow); 
```

#### GetLineWidth()

函数说明： 获得线宽

返回参数类型： `ELineWidth`

返回参数说明： 轨迹线宽

举    例：
```cpp
ELineWidth eLineWidth = pISnGraphics->GetLineWidth(); 
```

#### SetLineWidth()

函数说明： 设置线宽

输入类型： `ELineWidth`

输入说明： 轨迹线宽

举    例： 
```cpp
pISnGraphics->SetLineWidth(eLineWidth);
```

### 类IDisplayDistance

### 类ISnProjObjectAlt

### 类ISnProjConstantAlt

#### SetMax()

函数说明： 设置距离(扩展函数)

输入类型： `double`

输入说明： 最大距离值

举    例：
```cpp
pISnProjConstantAlt->SetMax(dmax);
```

#### GetMax()

函数说明： 获取距离(扩展函数)

返回参数类型： `double`

返回参数说明： 最大距离值

举    例：
```cpp
double dmax = pISnProjConstantAlt->GetMax();
```

### 类ISnProjection

#### SetDistanceType()

函数说明： 设置投影距离类型(扩展函数)

输入类型： `ESnProjectionDistanceType`

输入说明： 投影距离类型

举    例：
```cpp
pISnProjection->SetDistanceType(type);
```

#### GetDistanceType()

函数说明： 获取投影距离类型(扩展函数)

返回参数类型： `ESnProjectionDistanceType`

返回参数说明： 投影距离类型

举    例：
```cpp
ESnProjectionDistanceType type = pISnProjection->GetDistanceType();
```

#### GetDistanceData()

函数说明： 获取投影距离数据(扩展函数)

返回参数类型： `IDisplayDistance*`

返回参数说明： 投影距离数据

举    例：
```cpp
IDisplayDistance* distanceData = pISnProjection->GetDistanceData();
```

## 传感器三维

### 类ISnVO

举    例：ISnVO* pISnVO = pISensor->GetVO();

说    明：pISensor为ISensor类指针

#### GetSpaceProjection()

函数说明： 获得三维空间投影属性

返回参数类型： `double`

返回参数说明： 三维空间投影属性

举    例：
```cpp
double nSpaceProjection = pISnVO->GetSpaceProjection();
```

#### SetSpaceProjection()

函数说明： 设置三维空间投影属性

输入类型： `double`

输入说明： 三维空间投影属性

举    例：
```cpp
pISnVO->SetSpaceProjection(1.0);
```

#### GetPulse()

函数说明： 获得三维向量属性

返回参数类型： `IVOVector*`

返回参数说明： 获得三维向量属性

举    例：
```cpp
ISnVOPulse* pISnVOPulse = pISnVO->GetPulse();
```

#### GetVector()

函数说明： 获得三维向量属性

返回参数类型： `IVOVector*`

返回参数说明： 获得三维向量属性

举    例：
```cpp
IVOVector* pIVOVector = pISnVO->GetVector();
```

### 类ISnVOPulse

举    例：ISnVOPulse* pISnVOPulse = pISnVO->GetPulse();

说    明：pISnVO为ISnVOPulse类指针

#### GetPulseVisible()

函数说明：获得是否显示

返回参数类型：`bool`

返回参数说明：是否显示

举    例：
```cpp
bool bPulseVisible = pISnVOPulse->GetPulseVisible();
```

#### SetPulseVisible()

函数说明：设置是否显示

输入类型：`bool`

输入说明：是否显示

举    例：
```cpp
pISnVOPulse->SetPulseVisible(bPulseVisible);
```

#### GetAmplitude()

函数说明：获得振幅

返回参数类型：`double`

返回参数说明：振幅

举    例：
```cpp
double dAmplitude = pISnVOPulse->GetAmplitude();
```

#### SetAmplitude()

函数说明：设置振幅

输入类型：`double`

输入说明：振幅

举    例：
```cpp
pISnVOPulse->SetAmplitude(dAmplitude);
```

#### GetLength()

函数说明：获得脉冲长度

返回参数类型：`double`

返回参数说明：脉冲长度

举    例：
```cpp
double dLength = pISnVOPulse->GetLength();
```

#### SetLength()

函数说明：设置脉冲长度

输入类型：`double`

输入说明：脉冲长度

举    例：
```cpp
pISnVOPulse->SetLength(dLength);
```

#### GetPreselFreq()

函数说明：获得频率类型

返回参数类型：`ESnVOPulseFrequencyPreset`

返回参数说明：频率类型

举    例：
```cpp
ESnVOPulseFrequencyPreset ePreselFreq = pISnVOPulse->GetPreselFreq();
```

#### SetPreselFreq()

函数说明：设置频率类型

输入类型：`ESnVOPulseFrequencyPreset`

输入说明：频率类型

举    例：
```cpp
pISnVOPulse->SetPreselFreq(ePreselFreq);
```

#### GetFreqValue()

函数说明：获得频率

返回参数类型：`double`

返回参数说明：频率

举    例：
```cpp
double dFreqValue= pISnVOPulse->GetFreqValue();
```

#### SetFreqValue()

函数说明：设置频率

输入类型：`double`

输入说明：频率

举    例：
```cpp
pISnVOPulse->SetFreqValue(dFreqValue);
```

#### GetFreqReverseDir()

函数说明：获得是否反向

返回参数类型：`bool`

返回参数说明：是否反向

举    例：
```cpp
bool bFreqReverseDir= pISnVOPulse->GetFreqReverseDir();
```

#### SetFreqReverseDir()

函数说明：设置是否反向

输入类型：`bool`

输入说明：是否反向

举    例：
```cpp
pISnVOPulse->SetFreqReverseDir(bFreqReverseDir);
```

#### ResetToDefaults()

函数说明：重置为缺省值

举    例：
```cpp
pISnVOPulse->ResetToDefaults();
```

