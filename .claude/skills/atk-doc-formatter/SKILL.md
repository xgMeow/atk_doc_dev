---
name: atk-doc-formatter
description: 规范化 Markdown 文档格式（标题、按钮、对话框、图片、表格、公式等），使文档结构清晰、逻辑分明。用于 ATK 相关文档或一般技术文档的格式优化。
---

# ATK 文档结构格式化工具

## 何时使用

当用户提出以下任一请求时，应激活此 skill：

- “请格式化这篇文档”
- “按规范整理这个 Markdown 文件”
- “优化文档结构，使其更易读”
- 用户提供 Markdown 文档并期望统一的格式风格（特别是包含 ATK 软件操作步骤的文档）
- 文档中存在格式混乱：标题带编号、按钮未标记、对话框名称未用【】、公式格式不统一等

## 核心原则

在执行任何格式化操作前，始终牢记：

1. **只改格式，不改内容**：原文意思完整保留
2. **不私自改链接**：所有原有链接保持原样
3. **不私自改表达**：用户原始描述方式保留，只优化结构
4. **不进行内容拓展**：不添加原文档中没有的信息

## 格式化指令

按以下顺序处理文档。每一条规则都给出了“原格式 → 目标格式”的明确转换方式。

### 1. 标题（Headings）

- 移除标题前的编号（如 `1.`、`一、`、`1.1`）
- 标题层级不可跳级（例如 `#` 后必须是 `##`，不能直接 `###`）

**转换示例**：
```markdown
# 1. 创建任务场景   →   # 创建任务场景
### 三级标题（前面缺少##） → 需补充 ## 二级标题，再保留 ### 三级标题
```

### 2. 按钮（Buttons）

- **操作按钮**：用 `<kbd>` 标签包裹按钮文字
- **例外**：按钮作为标题或列表项标题时，**不**使用 `<kbd>`

**转换对照**：

```markdown
点击 新建 按钮   →   点击 <kbd>新建</kbd> 按钮
## 新建（作为标题）   →   保持不变（无kbd）
1. 添加卫星（列表项标题）   →   保持不变（无kbd）
在【开始】栏点击 插入   →   在【开始】栏点击 <kbd>插入</kbd>
```

### 3. 窗口与对话框（Windows/Dialogs）

- 具体的窗口、对话框、页面名称用【】包裹
- 通用的“对话框”、“窗口”等词汇不处理

**转换对照**：
```markdown
打开新建向导对话框   →   打开【新建向导】对话框
在属性窗口中   →   在【属性】窗口中
查看覆盖分析报告页面   →   查看【覆盖分析报告】页面
```

### 4. 图片（Images）

- **独立图片**：单独占一行的图片，统一使用 `![描述](路径)` 格式。
- **行内小图标**：与文字混排的小图标，统一使用 `<img src="..." no-view />` 格式。
- **空行要求**：独立图片的前后必须各有一个空行（与相邻段落、列表、代码块等隔离）。

**转换示例**：

```markdown
上一段文字（段落结尾）

![覆盖时段分析报告](./media/image30.png)

下一段文字（段落开头）
```

**行内图标示例**：

```markdown
点击 <img src="./media/add.png" alt="新增" no-view /> 可添加新项。
```

### 5. 段落与空行

- 段与段之间必须有空行，且只空一行
- 列表项之间不需要空行

### 6. 表格（Tables）

- 只有一列的表格 → 取消表格的结构，转换为文字描述的形式
- 多列表格 → 保留原表格结构

### 7. 加粗（Bold）

- 加粗文字前后必须有空格（除非紧贴标点符号）

**正确**：`这是 **粗体** 文字` 
**错误**：`这是**粗体**文字`

### 8. 公式（Formulas）

- 块级公式：`\[ ... \]` → `$$ ... $$`
- 行内公式：`\( ... \)` → `$ ... $`

**转换示例**：
```markdown
\[ E = mc^2 \]   →   $$ E = mc^2 $$
行内公式 \( a^2 \)   →   行内公式 $ a^2 $
```

### 9. 提醒块（Callouts）

将文档中的提示、注意、警告等关键信息转换为 `:::` 格式的提醒块，并根据内容补充恰当的标题。

**何时转换**：
- 原文包含以下关键词（或英文对应词）时，必须转换
- 原文使用普通引用（`>`）但内容属于提示/注意/警告等类型时，也应转换为提醒块，并移除 `>`

**关键词与提醒块类型对照**：

| 原文关键词                  | 目标格式        | 建议标题（根据内容归纳） |
| --------------------------- | --------------- | ------------------------ |
| **提示**：、**Tip**：       | `::: tip`       | 例如：“使用前准备”       |
| **注意**：、**Note**：      | `::: warning`   | 例如：“注意事项”         |
| **警告**：、**Warning**：   | `::: warning`   | 例如：“警告”             |
| **重要**：、**Important**： | `::: important` | 例如：“重要说明”         |
| **谨慎**：、**Caution**：   | `::: caution`   | 例如：“谨慎操作”         |

**转换示例 1（关键词转换）**：

```markdown
**注意**：使用前请先保存文件。
→
::: warning 使用前注意
使用前请先保存文件。
:::
```

**转换示例 2（普通引用转换）**：

```markdown
> 提示：网格检查前请确保已完成覆盖计算。
→
::: tip 使用网格检查工具的前提
网格检查前请确保已完成覆盖计算。
:::
```

**补充标题规则**：
- 如果原文已有 `:::` 提醒块但没有标题，根据内容补上一个简洁的标题
- 标题应概括该提醒的核心内容（如“注意事项”、“操作前确认”等）

### 10. 折叠块（Details）

当多步操作或长参数列表需要分组时，使用：

```markdown
::: details 标题（如“展开查看完整配置”）
折叠的内容
:::
```

### 11. 引号规范化（Quotation Mark Normalization）

**目标**：根据上下文，将文档中的双引号转换为正确形式：

- **中文文本**（包括中英文混合但以中文为主的语句）：使用成对的中文弯引号 **“**（左）和 **”**（右）。
- **代码、路径、命令行参数、英文专用术语**：保持直双引号 **"**（不区分左右）。

**处理规则**：

1. **识别上下文**  
   - 判断引号包裹的内容：若主要为中文字符（或虽含英文但整体是中文叙述），视为中文语境。  
   - 若内容出现在代码块、内联代码、文件路径、命令行参数、纯英文术语或键值对中，保持直双引号 `"`。

2. **转换动作**  
   - **中文语境**：  
     - 将直双引号 `"内容"` 转换为 `“内容”`。  
     - 若已有弯引号但方向错误（如 `”内容“`），纠正为 `“内容”`。  
   - **非中文语境**：保持直双引号 `"` 不变，不做任何转换。  
   - **不成对的情况**（如只有左引号或只有右引号）：根据语境如不正确，自动补全。

3. **禁止行为**  
   - 不得将代码/路径中的直双引号改为弯引号。  
   - 不得将正确的弯引号对改为直双引号。  

**转换示例**：

```markdown
# 原文（中文内容使用了直双引号）
参数值为 "测试"
→ 参数值为 “测试”

# 原文（中文内容使用了反向弯引号）
用户说：”你好“。
→ 用户说：“你好”。

# 原文（代码中的直双引号保持不变）
print("hello")
→ print("hello")

# 原文（文件路径中的直双引号）
--config="C:\config.json"
→ --config="C:\config.json"

# 原文（缺失左引号，不成对）
用户说：你好”
→ 用户说：“你好”
```

**特别说明**：  
- 此规则仅处理**成对出现的双引号**。单引号（‘ ’ 和 '）原则上参考相同逻辑，但由于文档中单引号错误较少，且易与撇号混淆，除非明显错误（如 `‘` 和 `'` 混用导致不配对），否则不作强制转换。  
- 转换时注意保持引号内原有文字不变，仅修改引号字符本身。

### 12. 文档描述（Description）

- 若文档 frontmatter 中缺少 `description` 字段，根据文档内容补充一个简洁的描述
- 描述应概括文档的核心内容，长度控制在 20-50 字
- 若已有 `description` 但描述不准确或过长的，优化为简洁版本

**转换示例**：
```markdown
---
title: 入门案例
---
→
---
title: 入门案例
description: 通过创建简单覆盖分析任务，快速上手 ATK 的基本操作流程。
---
```

## 完整工作流程

### 第1步：理解原文
- 完整阅读用户提供的文档
- 识别文档类型（案例教程、帮助文档、理论基础等）
- 确认是否有特殊要求

### 第2步：逐步格式化
按顺序应用上述13条规则（标题 → 按钮 → 窗口/对话框 → 图片 → 段落与空行 → 表格 → 加粗 → 公式 → 提醒块 → 折叠块 → 引号规范化 → 文档描述）。 
每应用一条规则，确保不破坏已有内容。

### 第3步：内容核对
- 逐段对比原文与格式化后的结果
- 确认没有遗漏、错改或添加内容

### 第4步：结构优化（可选）
- 如果文档较长，可添加内部的 Markdown 锚点链接（如 `[跳转](#标题)`）
- 为独立操作步骤增加折叠分组
- 确保提醒块放置在相关段落之前或之后，逻辑连贯

## 示例演示

### 示例1：操作步骤+按钮+对话框

**输入**：
```markdown
# 1. 创建场景

1. 运行 ATK.exe，点击"新建一个想定文件"；
2. 打开新建向导对话框，设置场景名称为 CoverageExample；
```

**输出**：
```markdown
# 创建场景

1. 运行 ATK.exe，点击 <kbd>新建一个想定文件</kbd>

2. 在【新建向导】对话框中，设置 **场景名称** 为 **CoverageExample**
```

### 示例2：单列表格转换 + 加粗空格

**输入**：
```markdown
| 数值 |
| ---- |
| 500  |

设置**高度**为500。
```

**输出**：
```markdown
- 数值：500

设置 **高度** 为500。
```

### 示例3：公式与提醒块

**输入**：
```markdown
计算公式为 \[ a^2 + b^2 = c^2 \]。
**注意**：使用前请备份文件。
```

**输出**：
```markdown
计算公式为 $$ a^2 + b^2 = c^2 $$。

::: warning 使用前注意
使用前请备份文件。
:::
```

## 边界情况与注意事项

- **标题中带有按钮**：不添加 `<kbd>`，保持原文字（例如 `## 新建`）
- **列表项中既有标题又有操作**：列表项开头的文字（作为该项标题）不加 `<kbd>`；描述中的具体按钮加 `<kbd>`。
- **空行**：注意不要引入多余空行（如列表项之间不需要空行，但段落之间必须有空行且只有一个空行）。
- **引号**：文档中的引号一般很少会出现错误，所以一定要理解上下文确定是否为误报。

## 验证清单

格式化完成后，用以下清单自检：

- [ ] 所有标题无编号，层级连续
- [ ] 操作按钮有 `<kbd>`，标题中的按钮无 `<kbd>`
- [ ] 具体窗口/对话框/页面名称有【】
- [ ] 块级公式为 `$$`，行内公式为 `$`
- [ ] 图片前后有空行
- [ ] 段落之间有空行，代码块前后有空行
- [ ] 没有单列表格
- [ ] 加粗文字前后有空格（除非紧邻标点）
- [ ] 提示/注意等语句已转换为 `:::` 块，且带有标题
- [ ] 中文弯引号左右方向正确（`“”`），未出现反向或两个右引号，一定按照这个标准来！不要自己猜测！
- [ ] 文档 frontmatter 有 `description`，内容简洁准确（20-50字）
